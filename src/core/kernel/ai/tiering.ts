/**
 * Compatibility module for root-fixed planning tiers.
 *
 * Tier selection is now request data validated through the behavior binding;
 * participant heuristics, reach constants and action-tag defaults are not AI
 * kernel semantics and are deliberately not retained here.
 */
export type { PlanningTier } from './types';
