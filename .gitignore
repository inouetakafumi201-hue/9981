node_modules
# 开发期临时产物（素材工作流管线样例/报告，见 scripts/asset-pipeline）
tmp/
probe12.mts
probe13.mts

# mutation-run.mjs 每次运行重建的运行产物（运行期输出，非仓库内容）
.mutation-report.json

# sprite-forge 出图密钥（本地 .env：gpt-image-2 / gemini 中转站 key，勿提交）
.agents/skills/sprite-forge/tools/.env

# 开发板/资产构建产物（Vite outDir；构建期生成，非仓库内容）
dist/

# 运行期产物（asset-pipeline 自测的 run/ 目录等）
run/

# Python 字节码缓存（sprite-forge 工具脚本运行生成，非仓库内容）
__pycache__/

# sprite-forge selftest 运行产物（raw/processed 输出，非仓库内容）
.agents/skills/sprite-forge/tools/selftest-run/

