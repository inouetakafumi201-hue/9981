# 地图包主链全量验收

> 状态：主链契约已冻结；主链定向验收通过，全仓既有红灯仍阻断正式素材批量生成
> 日期：2026-09-06
> 裁决来源：D-088、D-091
> 范围：设计与主链闭环；不批量生成正式素材

## 一、唯一生产入口

局外出租屋与局内地图不再拥有两套地图真身。二者统一从完整 `MapBundle` 进入：

```text
发现 manifest
  → 校验包内规范相对路径、依赖、版本、资源存在性与 sha256
  → 解析 Canonical MapData v2 + MapPlay 2.0 + PresentationProfile
  → compileMap
  → prefab.spawn
  → UiSystem projection
  → intent / result / snapshot 通信
```

会话差异只由 `sessionKind`、MapPlay、PresentationProfile 和宿主生命周期表达。出租屋可使用少 UI profile，但少 UI 不是第二套运行时，也不允许手写替代 MapData、实体或交互规则。

## 二、MapBundle 1.0 冻结字段

| 分区 | 必需内容 | 拒绝条件 |
|---|---|---|
| 身份 | `schemaVersion`、`bundleId`、`mapId`、`sessionKind` | 版本不兼容、bundle/manifest/map 身份不一致 |
| 清单 | `ContentManifest(contentKind=map-bundle)` | 重复条目、执行资源、非法 checksum |
| 地图 | Canonical `MapData v2` 与 `entryNodeId` | 入口不存在、地图 id 不一致 |
| 玩法 | `MapPlay 2.0` 与 mapBinding | 指向其他 bundle/入口、直接写规则状态 |
| 表现 | `PresentationProfile` | 缺条目或无法通过 profile 门禁 |
| 资源 | `visualAssetEntryIds`、包内相对路径、sha256 | URL/绝对路径/反斜杠/空段/`.`/`..`、缺文件、checksum 漂移 |

SVG 底图必须作为 `map-visual-asset` 放在包内，由 `MapData.backdrop.image` 或层级 backdrop 使用相对路径引用。禁止 base64 内嵌、blob URL、包外网络回退。

## 三、八类素材与过渡场景

唯一素材逻辑分类沿用已合并地图编辑器设计：`AI 单位 / NPC / 载具 / 容器 / 物品 / 机关装置 / 装饰 / 过渡场景`。展示标签不得驱动逻辑；旧六类只在导入边界按 D-088 的确定性表迁移。

过渡场景是 `MapEdge.transitionInstances[]` 的端点侧实例，每条边 `from`/`to` 各最多一个。边级参数共享，端点实例效果独立；删除、拆分、反向、复制和旧 `transitionWindow` 导入必须保持拓扑引用与迁移诊断完整。

## 四、通信协议 1.0

所有 UI 行为先由当前投影构造 `InteractionIntent`，再封装为 `intent` 包络。包络必须携带：`protocolVersion`、`sessionId`、`mapBundleId`、`requestId`、`idempotencyKey` 与修订令牌。

- `accepted` 必须返回 `committedRevision`；客户端观察到包含该修订的投影后才算完成。
- `rejected` 返回可见性安全诊断，不产生补偿写入。
- `stale` 进入重同步，旧意图不得自动改写后重放。
- 同一 session + bundle + idempotencyKey 只执行一次；同键不同 requestId 为冲突。
- 首次连接、修订漂移或指纹不一致必须请求全量 snapshot；增量投影不能跨 revision gap 猜测。

## 五、验收矩阵

| 能力 | 权威文档 | 实现/证据 | 当前结论 |
|---|---|---|---|
| 八类素材、端点过渡场景 | `创作系统/08`、D-088 | `asset:components` 生成 8 项目录；组件网格自测与 V2 Python 10 测试通过 | 已实现，正式批量生成仍阻断 |
| MapBundle 路径与 checksum | 本文、D-091 | residence/match 两个真实内存资源包，SVG 相对路径与真实 SHA-256 经 `loadMapBundle` 校验 | 定向测试通过 |
| 局内/局外统一装载 | 本文、`工程治理/11` | `loadMapBundle → createLoadedMatch → compileMap → prefab.spawn → UiSystem projection` | 双夹具通过定向测试 |
| 意图、幂等、重连、快照 | 本文、`表现系统/15` | `transport.test.ts` 覆盖 accepted/rejected/stale、同键冲突、首次连接、revision gap、指纹漂移 | 7 项测试通过 |
| 素材生成→校验→编译→spawn | `创作系统/03` | `asset:generate`、`asset:pipeline`、`asset:view`、`asset:components` | 通过：5 节点/4 边/2 实体，0 结构/引用错误 |
| 全仓质量 | `00_主状态板` | typecheck、typecheck:l2、lint、verify:data、verify:docs、test、build、devboard:build | 除全量 test 外通过；lint 为 0 error、有既有 warning |

## 六、浏览器验收（1020×741，深色）

| 入口/主路径 | 运行证据 | 结果 |
|---|---|---|
| 地图编辑器 `/map-editor` | `map-editor-dark-1020x741.png`；编辑器、八类快速素材库、校验面板可见 | 通过，无控制台 error |
| 素材库 `/asset-library` | `asset-library-dark-1020x741.png`；八类筛选、详情、回编辑器入口可见 | 通过，无控制台 error |
| 局外出租屋 | 标题菜单→开始新游戏→出租屋；`residence-dark-1020x741.png` | 通过，无空白页/断链 |
| 局内地图 | 出租屋→竞技匹配→床 A→确认→HUD；`match-hud-dark-1020x741.png` | 地图与 HUD 可见，但开发问题徽标显示 11 issues，未满足最终放行 |

截图保存在 `/tmp/agent-browser/`，不进入仓库。控制台仅见 HMR、React DevTools 提示与音频 no-op 日志，未见运行时 error；局内 11 issues 仍须归因清零。

## 七、2026-09-06 门禁结果

- 通过：`typecheck`、`typecheck:l2`、`lint`（0 error，既有 warning）、`verify:data`（89 JSON）、`verify:docs`、`build`、`devboard:build`。
- 通过：地图包/通信定向测试 17 项；`asset:generate`、`asset:pipeline`、`asset:view`、`asset:components`；sprite 组件网格自测、sprite 后处理自测、V2 Python 10 项。
- 阻断：`npm run test` 仍有既有 AI 提交/调参断言、归档后文档纪律基线、typecheck 覆盖守卫、UGC/UI 架构术语及素材导入排序等红灯。复现命令：`npm run test`。
- 环境说明：系统 Python 受 PEP 668 管理，Python 验收使用 `uv run --with numpy --with Pillow --with pytest` 的隔离环境完成，未修改仓库依赖。

## 八、放行规则

只有素材门禁与全仓 typecheck、lint、test、build、数据/文档一致性全部通过，主状态板才可标记“允许进入正式素材批量生成与局内/局外地图生产阶段”。本轮因全量 test 与局内问题徽标未清零，结论保持“禁止正式批量生成”；禁止用 fallback、旧快照或局外手写特例掩盖。
