# 图形化实现落点（镜头 / 声音 / 图层 → 技术栈映射）

> **性质**：**实现落点参考表（不是权威契约、不新增规则）**。`08_技术栈与代码生成画面.md` 的 2.4 设计落点映射表只覆盖了对局 HUD / 叙事模块 / 动效与粒子的**效果层**，但**镜头 / 声音 / 图层三大自主子系统的设计文档有大量具体但「没标工具」的参数**——它们写清了 lerp 系数、透明度函数、衰减公式、抖动振幅，却没说明"这个效果用哪个已定库实现"。本文件把这三份自立设计的技术化部分，按 `08` 的"每个效果指定工具、AI 按此对号入座"纪律逐条落到技术栈，让看的人拿到一个可实现的手册。**权威判断仍回各自主文档与 `01/02/03`**。
> **最后更新**：2026-08-16
> **依赖**：`08_技术栈与代码生成画面.md`（2.4 落点映射）、`02_镜头动态设计.md`、`03_声音动态设计.md`、`04_图层动态设计.md`、`01_图形化与UI.md`（对局 HUD 组件拆解 §三·五 + 视觉定律）、`06_叙事对话框系统.md`、`07_RPG叙事与引导系统全览.md`

---

## 〇、读法

08 §2.4 的用法是"AI 在表现层写任何东西前先查这张表；表里没写的效果先登记再实现"。三大子系统的设计文档给出了**参数**，本表给出这些参数对应的**库与数据形状**。逐行对号：拿到一个设计参数 → 查本表 → 落到库。表里没写的新效果 → 按 08 纪律先登记再实现，不即兴造轮子。

**总原则（承接 08 六条硬纪律）**：动效一律 Framer Motion；粒子一律 tsparticles；图标一律 lucide-react；可访问行为一律 Radix；音效一律 Howler；**禁止手写 CSS 过渡做线性淡入淡出做 UI 动效**（CSS 过渡只允许 hover/focus 微状态）。

---

## 一、镜头子系统（`02_镜头动态设计.md`）→ 库落点

镜头是一个**运行时状态机 + 纯函数变换**，不需要任何动效库——它就是 `zustand` store 只管状态、一个**渲染循环驱动的变换函数**把状态投影到 CSS `transform`。关键是把设计里的 lerp / 缩放 / 缓动用**声明式代码**表达，不要用 Framer Motion 的 `animate` 驱动镜头（镜头是连续高频跟随，不是一次入/出场）。

| 设计参数（02 出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 四种模式状态机（对局焦点/观战自由/编辑器诊断/关键演出锁定，§一） | 模式枚举 + 切换规则（Tab/WASD/演出锁） | `zustand` | `cameraStore`：`mode`, `followTarget`, `zoom`, `offset`, `overrides`；切换前后用声明式状态迁移，不瞬时跳帧 |
| 平滑追随 `lerp(pos, target, 0.15)`（§2.1） | 每帧位置插值 | 渲染循环（`requestAnimationFrame`）+ **不要**用 DOM 动效库 | 一个 `useRaf` hook 每帧读取 `cameraStore.follow()` 返回的 lerp 结果，经 `transform: translate3d(...)` 写到一个顶层容器（GPU 合成） |
| 边缘软推（进边缘 20% 时 lerp 系数 0.15→0.35，§2.1） | 阈值分支的插值系数切换 | 同一渲染循环里的纯函数 | `followEasing(distanceToEdge)` 返回平滑变化的系数（可再用一个 lerp 让它不突兀切变） |
| 自动缩放 `clamp(100% - d*5%, 60%, 100%)` 单/双/多目标（§2.2） | 视距→缩放档位（40%/60%/100%）切换 | 渲染循环 + 缩放用 CSS `scale` | 缩放变化用轻缓动（easeOut 0.5s）包一层，**不用 Framer Motion 的 spring**（镜头缩放是跟随不是跳变，spring 会多余回弹） |
| 焦点切换过渡（同场景 1s ease-in-out / 跨场景 0.3s ease-out / 选项淡入淡出，§2.3） | 过渡时长与缓动 | Framer Motion 的 `animate` 包一次位移 | 只在**轮权交接**这一离散事件上用 `animate`：`animate(cameraRoot, { x, scale }, { duration: 1, ease: 'easeInOut' })`；跨场景用 `duration: 0.3, ease: 'easeOut'` + 一个 0.1s 的黑屏 `AnimatePresence` 遮罩 |
| 观战自由 WASD / 拖拽移动（§3.2） | 输入→偏移量累积 | React 键盘/鼠标事件 + `zustand` | `useEffect` 挂 `keydown`/`pointermove`，写 `cameraStore.offset`；Shift 提速（×2） |
| 关键演出预设机位（Close-up 150% / Two-shot 120% / Env 100% / Top-down 40%，§4.2） | 固定机位数据 + 锁输入 | `zustand` + 一个可跳过的全屏演出时间轴 | 机位 = `cameraStore.preset(position, zoom)`；跳过用 `CeremonialTimeline`（`03` §三），锁输入 = 演出期间吞掉输入事件 |
| 屏幕抖动 轻±2/中±5/强±10 像素（§6.2） | 3 档振幅/频率/衰减 + 可关闭 | 渲染循环里的叠加噪声（**不**用 tsparticles） | 一个 `useScreenShake` hook：`trauma` 值衰减（ease-out），每帧采样 `(sin(t*freq)*amp)` 加到 transform 的 X/Y；`prefers-reduced-motion` 或设置 0% 时直接跳过 |
| 镜头边界软约束 + 橙色偏离蒙版（§7） | 边界限位 + 蒙版透明度 | 纯函数 + `zustand` | 边界 `clamp` 镜头坐标；偏离>2 屏宽时 `overrides.drifting = true`，画一个 `motion.div` 橙色渐变蒙版，透明度按偏离度（最浓 30%） |
| 图层高亮（聚焦对象所在层 100% 不透明，§8.2） | 透明度覆盖 | 交给图层子系统（见本文件 §三） | 镜头不写图层状态，只把"当前焦点 height"喂给图层的透明度缓存 |
| 视锥剔除 / LOD（§9.2/9.3） | 只渲染视野内、按缩放省细节 | React 渲染级（`useMemo` 可见集合） | 缩放<50% 时从可见集合剔除粒子与曲线细节，只留图标；>100% 时编辑器加网格线（原生 SVG） |
| Z 轴移动（编辑器 Q/E，§5.1） | 编辑器专用 Z 偏移 | `zustand` + 显示 | `cameraStore.z`, 左上角 `Z: +2.5` 文本；对局内禁用 |

> **镜头纪律**：镜头是高频连续跟随，**绝不用 Framer Motion 的 `whileInView`/`variants` 驱动它**——那是入/出场的一次性动画，不是跟随。过渡只在轮权交接这种离散事件用 `animate`，平时是一条渲染循环的纯函数插值链。

---

## 二、声音子系统（`03_声音动态设计.md`）→ 库落点

声音子系统已有相当具体的参数（四层混音、距离衰减公式、音高随机、连播上限）。它的实现载体就是 08 已定的 **`howler`**，外加一套**"语义事件 → 播放"**的解耦层（承接 `02` §8 声音语义描述符 + 规则事件不进入 NPC 感知）。

| 设计参数（03 出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 四层混音（关键反馈 100%/战斗 70%/环境 40%/UI 50%，§2.1） | 音量分层 + 各层连播上限 | `howler` | 每个 `Howl` 的分层 `volume` 乘数写死；用一个 `audioBus` 单例如：`key_feedback` 层最多 3 个实例，超限 kill 最旧 |
| 跨层总音量压缩（>100% 时按比例降其他层，§2.2） | 总输出限幅 | `Howler.volume()` 全局 | 每帧累加各层实时音量，>1 时给非关键层乘 `1/total`；用一个 `AudioMixer` 纯函数算每层最终乘数 |
| 距离衰减 `base × (1 - d/R)²`（§4.2） | 3D 音量 + 左右声道 | `howler` 的 `stereo`/`pos` + 一个纯函数 | 规则事件带 `source.pos`, 表现层算 `pan = (source.x - camera.x) / screenWidth`，`vol = attenuate(d)`，`howl.stereo(pan); howl.volume(vol)` |
| 音高随机 ±5%（§3.3 脚步声/受击） | 播放前随机 `rate` | `howler` | `howl.rate(1 + (Math.random()-0.5)*0.1)`；枪声/爆炸不随机（识别特征） |
| 环境音动态（平静/警戒/激烈/濒死，§5.2） | 按游戏状态切环境层音量与音轨 | `howler` + `zustand` | `audioStore.gameState`；两个 `Howl` BGM 交叉淡入（旧 `fade(0)` + 新 `fade(vol)`），环境层体积按状态表 |
| 视听音效提示（无障碍，§9.2） | 音源方向屏幕箭头 + 振动 | 原生 DOM/SVG（箭头）+ Web Audio（振动）或手柄 API | 箭头用 lucide 图标 + 位置跟随音源投影；开"减少突发音效"时 `Howl.volume` 压限 |
| 音效文件加载/卸载（§8.3） | 预加载对局音效、卸载非当前 | `howler` 的 `Howl.load`/`unload` | 对局开始 preload 一/二/四层 + 当前地图环境音；结束时 `Howl.unload` 只留 UI 层 |
| 字幕/语音（§7.3） | 底部居中字幕 + 名字色 | 原生 React + `zustand` + `howler` | 走 06 对话框模块的字幕槽（见 `06` §七.3），字数→时长表；玩家=青/NPC=灰白 |
| 换弹三段音效 + 曲线同步（§3.2 例外） | 分三段播放与曲线动画对齐 | `howler` 返回 id 可 `stop` | `03` 的 M 组换弹曲线是物品局部旋转（枪口下倾抖两下），音效 0.5+0.5+0.5 三段各在曲线阶段起点触发，用 `howl.seek()`/`id` 精确对齐 |

> **声音纪律**：声音由**规则事件触发**（`after:*` 事件），不等动画；表现层只 `Audio.play(soundId)` 不写任何规则状态（`02` §8.3、`03` §十）。所有音效（包括无害的 UI 音）都区分"规则可感知事件"vs"纯 UI 音"，后者不进 NPC 感知。

---

## 三、图层子系统（`04_图层动态设计.md`）→ 库落点

图层是最"数据驱动"的子系统——它本质是**每个实体挂一个 height 值，表现层用透明度函数换算 CSS opacity**。核心是一个纯函数 + 一个派生渲染，不需要任何动效库的"动画"（透明度切换走 CSS transition / Framer Motion 的 `animate` 过渡）。

| 设计参数（04 出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| height 模型 + 透明度函数 `opacity = clamp(0, 1 - |Δh|×0.1)`（§1.2） | 实体→图层→高度 的派生表 | 纯 TS 函数（可单测） | 一个 `layerOpacity(entityHeight, focusHeight)` 纯函数，被所有实体渲染读；`Δh≤9` 交互高亮时返回 `1`（覆盖函数，见 §2.4） |
| 三界之外独立层 `height = null` 恒不透明（§1.2/§2.1） | 标记为 UI/特效/对话框的实体不做透明度处理 | 一个 `layerTag` 元标记 | `height === null` 的对象跳过透明度函数，恒 `opacity: 1`（但可被自身动效单独控制） |
| 焦点图层判定（对局焦点跟随行动主体 / 观战点角色 / 编辑器选中，§2.2） | 当前 focus height | `zustand` | `layerStore.activeHeight`；轮权交接/位移/点角色时更新，配透明度 0.5s 过渡 |
| 透明度过渡 0.5s ease-in-out（§2.2 焦点切换） | 图层批量的透明度平滑过渡 | CSS `transition: opacity .5s ease-in-out` 在实体内 | 不是每帧重算——切焦点时各实体 `opacity` 走一次 CSS 过渡；`prefers-reduced-motion` 时改为即时 |
| 跨层交互高亮 `Δh≤9` 优先于透明度（§2.4） | 规则层 `canInteract` + Δh 判定 → 覆盖 opacity | 纯函数 + 复用 `01` 边缘发光 | `canInteract(target) && |Δh|≤9` → `opacity: 1` + 边缘发光（tokens 色）；>9 → 不渲染 |
| 跨层动画四类：坠落/位移/楼梯/传送（§4） | 高度连续变化的动画 | Framer Motion `animate` + 曲线配方（`03`） | 坠落=下坠位移曲线 + 各层透明度实时重算（每帧 `layerOpacity(height)`）；楼梯=P1 跳跃 + S3 阶梯缩放；电梯=匀速 `animate` + 人物姿态不动 |
| 编辑器"当前图层特权"（当前+以下可见，以上隐藏，§3.1） | 编辑器视图的图层过滤 | 纯函数 + `zustand` | `editorStore.currentLayerId`；渲染层不再产出高于当前层的对象，低于的半透明（`04` §3.4） |
| 贴纸锁定 + 移动图层（剪切/粘贴或右键菜单，§3.3） | 图层归属的编辑操作 | Radix `dropdown-menu` | 右键菜单"移动到图层..."；锁定 = 拖拽不使用图层命中，改走菜单 |
| 透明度预计算缓存（§7.2 `OPACITY_TABLE`） | Δh 0-10 查找表 | 常量数组 | 直接用 `04` §7.2 的 `OPACITY_TABLE`，避免逐实体重复算 `min/max` |
| 渲染剔除（Δh≥10 不渲染，§7.1） | 可见集合过滤 | React `useMemo` | 渲染循环里只产出 `|Δh|≤9` 或 `height===null` 的对象 + 当前层；性能预算内 |
| 图层切换提示（中央"2F" 1s 淡出 + 上升/下降音，§9.2） | 过渡演出 | Framer Motion `AnimatePresence` + `howler` | 切层时中央 `AnimatePresence` 显示层名，`translateY+scale` 入场、淡出 1s；上升音阶如何尔（见本文件 §二声音） |

> **图层纪律**：图层是**纯视觉派生**，`layerOpacity` 只读投影、永不反写规则（寻路/射线检测是 3D 几何，见 `04` §五）。写新特效时颜色一律走 tokens 语义色，不新增主色。

---

## 三·五、对局 HUD 组件拆解（核心：轮次栏 / 动作菜单 / 投点滑块 / 状态栏 / 物品栏）

> 08 §2.4.1 已把对局 HUD 的**效果层**钉了库（轮次栏 `layout` 重排、数值条 `useSpring`、玩家框反光 `boxShadow`）。本节补的是 08 没落的**组件怎么拆**：一个 UI 组件由哪些子状态、哪些 zustand store、哪些 Framer Motion 变量、哪些 Radix 原语构成。**规则事实永远来自玩法层只读投影，表现层只消费、不本地扣 AP / 推进状态**（01 §响应式不变量 5）。颜色一律走 tokens 语义色（01 §1 五条视觉定律），图标一律 lucide，动效一律 Framer Motion，可访问行为一律 Radix。

### A. 轮次栏（D-036，最核心组件）

**组件拆解**：轮次栏 = 一个 `TurnOrderBar` 组件，内含 `TurnFrame` 列表。它同时是状态栏、排名榜、动效舞台——但**动效不改规则**，所有排序/高亮都读只读投影重新派生，绝不本地改排名。

| 子状态 / 行为 | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 轮次框列表（玩家头像+名字+排名） | 按键行动轮排名排列 + 每次变化立即重排带 `layout` 动画 | Framer Motion `layout`（08 已钉）+ `zustand` | `turnStore` 存只读投影的排名数组；`motion.div` 列表项加 `layout`，排序变化自动过渡 |
| 当前行动方高亮 = **反光感**（glossy）不是描边 | 材质高光：从顶到下的渐变光带 + 轻微 scale | CSS（tokens 光亮色）+ Framer Motion `animate={{scale:1.02}}`（08 已钉） | 高亮用 `background: linear-gradient(...)` + `boxShadow inset` 做反光条；`whileLayout` 时并行动画，不动用粒子 |
| 玩家自己的框 > 他人约一骰宽 + 条更粗 | 尺寸差异化（骰宽≈图标宽度，用 tokens 间距常量） | Tailwind + tokens | 自己框 `width: base + diceWidth`，数值条 `height` 略增；颜色规则与他人一致（不换语义色） |
| 生命 5 格红 / 清醒 5 格蓝（离散分段） | 每格一个独立方块，变化走 `useSpring` 宽度 | Framer Motion `useSpring`/`useTransform`（08 已钉） | 受击红格"闪烁→消失"，恢复灰格"填彩"（01 §柱状图动画）；清醒格降透明度耗尽 |
| 已行动者变灰降饱和（D-035，**不显著**） | 整框降饱和，**不加大叉/不加"已结束"横幅** | CSS `filter: saturate(.4)` + `opacity` | 只是 `desaturate` 一层，保持可读但不抢眼；轮值状态用 tokens `cooldown` 灰 |
| 投点骰子悬空（只在投点时出现，右侧） | 条件渲染一个悬空骰子图标 + 序号块（1-5） | lucide `Dice` + 原生 div | 只在"统一投点/行动投点"时于当前行动者框右侧 `AnimatePresence` 出现；平时 `display:none`，**不以 DOM 恒在**（低心智，不在时就是不在） |
| 投射条比较面板（右侧一排横条） | 横条从各自序号右侧伸出 + 上移避让面板 | Framer Motion `animate` 宽度 | 横条默认灰白，`AnimatePresence` 批量进出；面板伸出时整体上移一个 `reserveSpace` 高度，避免盖右下动作面板（01 §骰子横条面板适配） |

**轮次栏数据纪律**：`turnStore` 必须是**只读投影的镜像**，来源是玩法层权威行动轮。排序变化（逆转/处决/弱点/招架）都来自 `after:*` 事件或投影变更，表现层不推断"谁行动过了"。

### B. 动作菜单（两套独立 + 上下文菜单 + 倒计时）

| 子状态 / 行为 | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 付费动作面板（1 AP，铁律 0 与 1 AP 分离） | 有费用菜单 + 切换零费按钮 | Radix 单选项列表（`radiogroup`/`dropdown-menu`）+ Tailwind 卡片 | 每张动作卡 = 图标 + 文字 + 底部菱形数字；不足 AP 用扁平灰、无高光（`tokens` `cooldown` + 高光仅 on-hover） |
| 附赠动作面板（0 AP，依附） | 切换按钮触发、与付费菜单互斥显示 | zustand（`menuTab: 'paid'|'free'`）+ Radix | 两面板**不同时渲染**；0 AP 卡顶部标 `green` 免费语义 |
| 动作卡底部菱形数字 = 成本（保留，08-15 定案） | 橙色菱形=AP、蓝色菱形=SP；**2 AP 的"中间过程"动作不成灰** | lucide `Diamond` + tokens | `2` 语义 = 中间过程（撬锁）第一次选消耗 2、表现连贯做完两步，逻辑解耦；数值/颜色走 tokens |
| 剩余 AP 显示 + 自动筛选可用动作 | 头 `可用动作（剩余 AP: n）` + 只列可用 | zustand 派生 + 只读投影 | 投影给"可动作 + 各自成本"，可行/不可行由表现层按投影标记（不可行=灰，**不动规则**） |
| 上下文动作菜单（点击目标弹出） | 点击实体/物品/载具/倒地敌人弹上下文菜单 | Radix `dropdown-menu` + 点击命中 | 菜单项 = {动作, AP 成本}；`命其长眠`用蓝色闪烁高亮；`查看装备`标 0 AP/不消耗 |
| AP 耗尽 → 只留零费菜单 + 3s 倒计时结束回合 | 自动切到零费 + 倒计时条 + 结束回合键 | zustand（`phase:'roundOver'`）+ Radix `progress` + howler | 倒计时 3→2→1 走 `requestAnimationFrame`；结束回合键点击 = 提交真实意图，**计时结束也自动提交**（01 §D-042） |
| 空手/无可用动作时 | 菜单不出现或只留零费 | zustand 派生 | 空手进场（D-041）→ 无付费动作，直接零费 + 结束回合 |

**可访问性**：付费/零费两菜单用 Radix 保证键盘可达，焦点随切换进入菜单、关闭回到触发对象（01 §输入等价）；数字键单选物品，↑↓+Enter 选动作（01 §键盘快捷键）。

### C. 投点阶段：强力骰 + 逆转 推挡滑块（关键）

| 子状态 / 行为 | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 两个推挡滑块（强力骰 + 逆转），**2 个不可漏** | 每个是可推 4 档（0/1/2/3）的滑块，下标 0/1/2/2 | Radix `slider`（可访问，键盘可推）+ `drag` | 档位映射：3 档=标号 2 但位置靠后一档（`下标 0/1/2/2`），"极限档"视觉暗示用 CSS（档间距 `<` 其余） |
| 档位特效分级（越高越重，充能感/光效/浓度/粒子） | 每档不同强度表现 | Framer Motion + tsparticles（1→2 档增强）+ tokens | 0=静置灰白；1=轻微充能光晕；2=粒子残影 + 色浓度升；3=爆闪 + 粒子爆发（`emitter` 一次性）。**特效与档位一一对应，一眼看出激进程度**（01 §投点阶段 UI） |
| 强力骰 = 消耗清醒值（蓝菱形）；逆转 = 消耗 SP/反制（紫侧） | 滑块颜色/语义区分 | tokens：强力骰走 `balance`/蓝，逆转走 `constraint`/紫 | 滑块 label 上方当前选择摘要："+2 修正（消耗 2 清醒）"、"档位 2（反制更强）" |
| 投出 → 骰子悬空 + 横条三段式生长 | 灰白伸出 → 强力骰特效+快速伸长 → 统一从左侧刷色 + 强力骰段泛红 | Framer Motion 序列（`useAnimate`）+ CSS 渐变刷入 | 三段 `animate` 按序跑：① `width:0→n` 灰白；② 强力骰玩家的条 `easeOut` 更快 + 右侧 `+1/+2` 角标；③ 统一从左边 `linear-gradient` 刷色（AP 色：0灰/1绿/2橙/3红），强力骰多出的段泛红 |
| 横条带格状刻度、无上限框 | 刻度网格 + 长度=点数 | 原生 div/`grid` 背景刻线 | 刻度显示投了几点，不画固定上限黑框（01 §横条有格状刻度、无上限框） |
| 投点播放：就地播放不独立弹窗 | 在轮次栏右侧就地放完 → 进入结算投影 | `zustand`（`dicePhase`）+ 轮次栏右侧舞台 | 结算面板不弹独立框；结果行"玩家 A：🎲4(+2)=6→2AP"直接在轮次栏旁的舞台投影（01 §D-036 就地播放） |
| 零挫败退还（消失蓝块瞬间恢复） | 退款动画：消耗的清醒格瞬间回满 | Framer Motion + 即时恢复 | 退还时蓝格`填充`立即恢复（不走慢速渐隐，01 §柱状图动画），配一声 UI 音 |

**滑块的"4 档审美强过逻辑"纪律**：**下标 0/1/2/2 是实现即契约**——第 4 档与第 3 档视觉距离更近（CSS 间距控制），不为逻辑正确性牺牲这个拥挤感。改滑块的视觉间距必须回 `01` 定案，不能随手调成均分。

### D. 状态栏 / 物品栏 / 头顶弱点 / 瞄准

| 子状态 / 行为 | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 状态图标栏（头像 + HP/SP/AP + 状态徽章） | 头像下 红 5 格生命 + 蓝 5 格清醒 + 橙 AP | Framer Motion（数值 `useSpring`）+ lucide | 复刻轮次栏的格状资源组件；状态徽章用 lucide + 语义色徽章（正面绿/中性灰白高光/负面按语义） |
| 头顶弱点图标（D-031 常驻，Z 5） | 角色头顶弱点图标 + 悬停克制说明 | lucide + Radix `tooltip` | 弱点图标常驻飘头顶；悬停显示克制关系补充；渲染在实体层之上（Z 近 5） |
| 瞄准紫色点线（D-033 低显著性，仅悬停） | 悬停角色显示一条紫点线到其瞄准目标 | 原生 SVG 虚线（`stroke-dasharray`）+ tokens 紫 | 仅悬停时出现，**无感叹号强提示**；招架则无任何标识（D-032 真隐藏），两者不混 |
| 物品栏槽位 + 拖拽交换 + 右键快捷菜单 | 4/6 格 + 拖拽换位 + 右键使用/丢弃/检查 | Radix `context-menu` + `dnd-kit`（后置档）+ zustand | 出生空手（D-041）全空；拖拽不合法落点回原位并说明（01 §输入等价/平板端）；右键菜单走 Radix `context-menu` |
| 负重提示（重装 ⚠️ + 来源） | 携带重物时顶显示 + 移动预览 AP 变化 | tokens + Framer Motion（出现动画） | `role`/`cost` 全来自投射，重量徽章用黄/橙告警语义 |
| 距离 DC 显示（枪械射击预览） | 攻击预览 + 命中 DC + 输出概率表 | 无新库（纯数据渲染）+ tokens | 从距离 DC 表查 DC，展示"投 d6≥4 命中(2伤)、≥6 翻倍(4伤)"；全程投影，不本地算 |

### E. 回合流程指示器 + NPC 阶段

| 子状态 / 行为 | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 顶部回合/阶段/当前方指示 | "回合 5｜玩家行动｜玩家 A" + 轮到时"你的回合！剩余 AP" | zustand + Framer Motion（`AnimatePresence` 切换） | 阶段推进来自投影；轮时用 `motion.div` 弹出 + 橙色 `⚡` |
| NPC 行动阶段标识 | "NPC 行动阶段｜僵尸移动中…" + 图标闪烁 + 快速结算 | lucide + Framer Motion（透明度闪烁） | NPC 结算不等玩家确认；移动路径用 SVG 线条动画 |

**对局 HUD 总纪律**：**组件全部读只读投影**，任何"谁行动过 / 谁有 AP / 谁锁定 / 谁重装"都不得本地推断或缓存后过期。**规则已经结算完才显示结果动画**（`after:*`，01 §动画与规则结算解耦）。动效是结果的视觉重演，不是并发推进。

---

## 三·五·补、AI 行为可视化（承接 `L3/02` 性格/难度 + `L3/03` M10 + `07` RPGNPC）

> 玩法层 docs/L3/02 定了**性格/难度两个正交 Profile**（所有字段 1–5），纯属"选择概率背后的权重"，**本人不可见、不进对局 UI**。它产生的可见面只有两个：**动作意图的可选展示**（`explanationDetail` 档，02 §5.5 明确"只影响调试/提示/可选意图展示，不改变决策；隐藏策略不得向对手泄露"）+ **NPC 的人格标签落点**（07 RPG 模块）。这一节是这些"AI 决策结果的可视化落点"，不是把神经网络的温度图做成 UI。

| 设计参数（出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| Profile 的 1–5 维度（温度/风险/合作/保守/坚持，L3/02 §二） | 纯内部权重，**不进对局 UI**；只在 debug 面板可选展示 | `zustand` + 可折叠 DebugPanel | 不上 HUD；debug 面板用 `lucide` slider/分数条显示 Profile 数值，`explanationDetail` 档决定展示详细度，**只在允许时**（不泄露隐藏策略于对手） |
| 动作意图的可选展示（L3/02 §5.5 + 07 RPG 目标追踪） | 每候选的语义标签（`risk:high`/`team:assist`/`resource:consume`）+ 决策理由 | Radix `tooltip` + lucide 标签 + tokens | 高 `explanationDetail` 时在动作卡/目标卡 hover 显示"AI 为何选这个"（语义标签 → 可读理由）；**默认关闭**，除非玩家设置打开且该 AI 允许 |
| explain detail 不泄露（L3/02 §5.5） | 隐藏策略（招架/埋伏）不因高解释档向对手泄露 | `zustand` `explainVisible` 门 + 可见性过滤 | 对局内对手视角**永不显示**对方 AI 的意图解释；只对自己/观战（若全场公开规则）可选开 |
| NPC 人格标签（07 RPG 引导） | NPC 头像/条旁挂人格徽章（谨慎/冒险） | lucide + Radix `tooltip` | 已由 07 任务日志/目标追踪模块承载的徽章统一走 lucide + tokens；人格标签是可略过的辅助信息，不是常驻 HUD |
| NPC/玩家 AI 行动阶段（回合指示器，L3/02 §五） | "NPC 行动阶段"+ 人格版路径显示 | 复用 §三·五 E 的回合指示器 | 不新组件；AI 行动时角色棋子走既有双段寻路/ORCA 视觉（01 §前端动态图形化），动作代价只在动作结算后显示 |
| 决策审计可回放（L3/02 §九 回放不变量） | 相同输入同一选择序列 | 无 UI 依赖；debug 面板 + `zustand` | 表现层只消费最终结果（哪个候选被选中），不画决策树；审计回放是开发期 `DesignCurrencyTrace`，不属对局 UI |
| 意图提交的交权（M10 状态机"整轮推进"，L3/03） | 回合驱动器驱动多步（睡下→起床/过载/倒地站起），交权时动画同步 | 复用 §三·五 E 交权（Framer Motion `animate`，`01` §交权硬同步） | AI 线回合驱动器推进五阶段时，表现层每个阶段结果动画走 `after:*` 再播；交权硬同步等演出停 |

> **AI 可视化纪律**：**Profile 权重本身绝不上 UI**（那不是玩家要读的东西，且属内部决策细节）；玩家能看到的只有「AI 下一步做什么 + 可选的理由 + NPC 人格标签」。任何把温度/分数/搜索深度画成 HUD 的做法都是把实现泄漏给玩家，违反 01 低心智性原则。debug 面板只在开发期、且不泄露隐藏策略**给对手**（招架等）时才展示。

---

## 四补、对局 HUD 的素材需求清单（承接 §六 分界）

| 对局 HUD 元素 | 代码动态 | 素材管线需求 |
|---|---|---|
| 轮次栏框 / 数值条 / 反光 / 横条 | 全代码（Framer Motion / CSS / tokens） | ❌ 纯代码 |
| 骰子图标 / 序号 / +N 角标 | lucide `Dice` + 原生 div + Framer Motion | ❌ 纯代码（不贴骰子 GIF） |
| 动作卡 / 上下文菜单 / 倒计时 / 滑块 / 物品格 | 全代码 + Radix | ❌ 纯代码（图标走 lucide / `05` 组件管线可补静态物品图） |
| 物品 / 武器 / 道具图标 | 格子内小图 | ⚠️ 需 `sprite-component.py`（`--context ui` 图标视角，`05` 规范）或复用既有素材 |
| 玩家 / NPC / 怪物头像 · 棋子 | 轮次栏头像小图 + 地图棋子 | ✅ 角色管线产物（`sprite-forge` 母版系统） |
| 弱点图标（头顶 + tooltip 引用） | lucide + 语义色 | ❌ 纯代码（可复用武器属性符号） |

---

## 四、叙事对话框与 RPG 模块 → 库落点（承接 08 §2.4 + 06/07）

06/07 已被 08 §2.4 的落点映射表覆盖得很完整（对话框斜切滑入、通知、任务日志、目标追踪、区域名、技能树全部钉了 Framer Motion / Radix / zustand / lucide）。这里只补 **06/07 尚未在 08 表里出现、但设计文档里明确写了的几个细节**，避免重复造表：

| 设计参数（出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 梦境侵蚀三态（淡白/泛白/纯白，L3/01 §9.4 + 01 纯白概念色） | 随缩圈的视觉渐变（褪色→失细节+回响→纯白光） | tsparticles（泛白雾/侵蚀粒子）+ CSS 滤镜 + Framer Motion（纯白闪光蒙版） | 淡白 = 场景 `filter: saturate(.7) contrast(.9)` + 轻噪点粒子；泛白 = 地面/边缘褪白 + 细节淡出 + 环境音压低（`howler`）+ 体力被推高时过载逼近感；纯白 = 全屏 `motion.div` 白光蒙版（`dream` 白 token）渐入，进入即迷失；三态递进由规则层 `condition:dream-threshold` 投影驱动，表现层只做视觉翻译 |
| 场景环境文本 / 道具描述的"只言片语"（叙事/00 §十四"首轮呈现基线"） | 环境可读文本 + 悬停道具描述 + 碎片线索附身物 | 06 对话框模块 + Radix `tooltip` + zustand（已收集线索派生） | 环境文本 = 悬停场景显示简述（走 06 tooltip 风格）；道具描述 = 悬停即显（复用 01 悬停提示）；碎片线索（门缝纸条/梦境残留）收集后进收藏室真相板（运营/04 §三）；**不播片、不弹长 PPT**——轻叙事重路，线索藏在悬停与只言片语里（叙事/00 §一） |
| 现实驻地"透明人"反差呈现（叙事/01 §一：现实透明/梦境传奇） | 现实侧低饱和静谧 vs 梦境侧高饱和张扬 | tokens 色温 + Framer Motion（冷暖切换）+ CSS | 现实侧整体 `saturate` 略降、静态冷调；梦境侧还原高饱和 + 动态粒子雨雪（01 梦境环境氛围）；两者视觉反差即"梦里是神现实是鬼"的镜头语言，不新增第二套色调 |
| 证据提交 = 三边身份叙事场所（叙事/01 §五 / 运营/05 §4.2.1） | 白虚空取证室 + 槽位 + 警官对话框反馈 | 已覆盖运营/05 §4.2.1（本文件 §五 VR 证据提交行） | 复用；警官反馈三层（常规/证据链/隐藏真相）走 06 对话框 + 青色标的反馈徽章（01 青色=交流/UGC）；格式化压抑光靠 `dream` 白 token + 低饱和 |
| 主线碎片"意外触发"（叙事/01 §六"无意识执念"） | 特定时机角色脱口而出一句"那个人"，说完愣一下 | 06 对话框 + Framer Motion（角色微抖/环境失焦） | 触发 = 对话框短句 + 角色头像顿一下（`scale` 微抖）+ 周围环境短暂失焦（`filter: blur` 再回）；**不是播片**，是自然流露的碎片化叙事（叙事/00 §一），可跳过 |
| 缩圈/梦境侵蚀的边界高亮（L3/01 §9.4 进入即迷失） | 纯白边界视觉防线 + 入侵警告 | Framer Motion + tsparticles（边界发白光） | 侵蚀边界用边缘发光白光 + 泛白粒子从外向内推进感；进入纯白区前红/白警示（01 →红=致命/纯白=梦境边界），警告只在逼近时出现不铺满 |

| 设计参数（出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 对话框立绘降级 + 等比缩放（06 §1.2/§6.2，含 `object-fit: contain` 的"完整显示不裁切"） | 图像加载/缩放容器 | 原生 React + 一个 `aspectRatio` 纯函数 | 立绘容器 `object-fit: contain`（不是 cover，见 06 明确"不裁切"）；降级链 = `imageRef → portraits[0] → sprites[0] → icon → 黑底名字首字母`，缓存结果 |
| 对话框 `z-index` 分层（立绘 100 / 对话框 200，06 §1.2） | 图层层级 | CSS `z-index` | 对话框是 `height=null` 三界之外独立层（06 §7.1），层级总是最上但不盖暂停菜单 |
| 打字机逐字 + 打字音 + 点击跳过（06 §2.3/§4.2） | 逐字定时器 + 音效 | `zustand` + `howler` + `requestAnimationFrame` | 打字机用 rAF 不用 setInterval（06 §8.2）；20 字符/秒；打字音在每字起点触发、类型限频防轰炸 |
| 定时对话框 autoAdvance 倒计时条（06 §2.3） | 3s 倒计时 UI | Radix `progress` 或原生 div + Framer Motion | 右下角圆形/条形进度，倒计时结束自动 `advance()`；点击跳过 |
| 选项快捷键 1-4 / 方向键 / Enter（06 §3.3） | 键盘导航 | Radix `radiogroup` 或自定义 + `useKey` | 数字键选选项，↑↓ 切高亮 + Enter 确认；复用动作菜单选择框样式（临时切换后还原，06 §3.3） |
| 多角色对话（06 §9 增强 B：顺序 `showDialog`） | 对话队列 | `zustand` `dialogStore` + `AnimatePresence` | 顺序 await；前端 `key={speaker+index}` 触发立绘淡出/淡入 |
| 区域名 / 任务卡片 / 通知的 P5 动态（07 §五） | 斜切滑入弹性 | Framer Motion（已钉 08 §2.4.1） | 照 08 表走：`skewX` + `type:'spring'`，全部 `transform` GPU 加速 |
| 表情系统（方案 C，PLT-01 §表情系统 + 纯黑脸双白眼约束） | 闭眼=运行时色块替换 + 复杂表情=预生成贴图叠加 | 纯 TS 像素合成 + Web Canvas / 离屏 canvas + zustand（表情状态）+ Framer Motion（表情切换微动） | 闭眼/眨眼：加载 sprite sheet 时做**一次**纯黑脸+双白眼检测（`RGB<10` 定位脸区、`RGB>245` 定位白眼坐标），**缓存锚点**运行时把白眼像素涂黑 → 闭眼（sleep/dizzy/blink 自动触，零资源、所有角色通用）；眉毛/复杂表情：`face_open/angry/surprised/tired` 32×32 透明贴图 `alpha blend` 到脸区坐标（`faceOpenStore` 换表情 + 微动）；状态 = `emotionStore`，进 `sleep`/`overload` 自动闭眼。**前端实现时**生效，不阻塞母版生成 |

> **表情系统纪律（PLT-01 定案）**：纯黑脸 + 双白眼是可编程表情的前提，**所有角色母版必须遵守 "纯黑脸 + 固定位置白眼" 约束**（UGC 角色自动继承，`PLT-01 §六 三条美术红线` 之一）。闭眼靠色块替换（零帧）、复杂表情靠贴图库（不占帧），不是为每个表情×姿态生成独立帧。这套是**运行时合成**，不是素材管线——素材只产出 16 姿态帧，表情在渲染时叠加，任何角色/任何 UGC 都能用同一表情库。

---

## 五、驻地与局外承载物动态 → 库落点（`运营系统/03/04` + `运营系统/05`）

驻地（出租屋）是**代码生成画面的主战场**，靠素材管线产出静态承载物帧 + 代码做入口高亮/演出的动态。以下把驻地各接触面的动态落到库：

| 设计/参数（出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 出租屋拓扑导航 + 实体入口高亮（运营/03 §二 + 03 灵感索 §六） | 可交互实体 + 边缘发光 + 少 UI 模式帧 | 原生 React + Framer Motion（hover 微状态）+ tokens | 每个承载物（床/匹配机/研究台/电脑/书架/保险箱/门缝）是一个静态组件帧（素材管线），悬停 = `drop-shadow` 边缘发光 + `hover:scale`, 入场淡入 |
| 匹配机彩色 UI + 床灰→可交互（运营/03 §四门控铁律） | 匹配状态 → 床可交互态切换 | Radix `dialog` + `zustand` | 匹配完成前床 `disabled`（扁平无高光，`cooldown` 灰）；完成后床高光亮起 + 边缘发光 |
| 影子大厅（运营/03 §五，不重载） | 站桩影子/粒子轮廓 | Framer Motion + tsparticles（低密度） | 只叠加"淡影子 + 白/淡粒子轮廓"，不建场景；匹配中可继续活动（zustand 不阻塞） |
| 纯白显形传送演出（运营/03 §六状态机） | 1s 纯白 → 推向床 → 复原色 | 声明式 timeline + Framer Motion + 全屏蒙版 | `CeremonialTimeline` 或 Framer Motion `animate` 序列：纯白全屏蒙版渐入 → 人物纯白 + 位移 → 床方向颜色滚动 → 复原；可跳过 |
| 保险箱抽屉感（运营/04 §二） | 抽屉平铺 + 翻抽屉"贵重感" | Framer Motion `layout` + 高光 + `howler` | 抽屉卡片 `layout`（展开动画），翻动 = `rotateX`/`translateY` 轻微翻起 + 金光高光（tokens `gold`）+ 抽别"沉重滑开"曲线 + 音 |
| 书架星标置顶 + 与编辑器高频切换（运营/04 §三） | 星标排序 + 往返动画 | `zustand`（星标排序派生）+ Framer Motion | 星标集 = `bookmarkStore`；素材卡 `layout` 重排；往返编辑器用一组"切换备用动画"（`AnimatePresence` 页面级） |
| 快捷素材栏 7 格→70 格矩阵（运营/04 §3.4，拖拽） | 展开动画 + 拖拽工具 | Framer Motion（展开）+ `dnd-kit`（拖拽，08 后置档） | 展开 = `layout` + 高度过渡；拖素材 = `dnd-kit`，且切放置素材动作（画图元笔自动放下，见创作/01） |
| VR 证据提交 = 复用一个独立图层（运营/05 §4.2.1） | 白虚空全屏层 + 暗桌 + 警官 + 槽位 | `zustand` + Framer Motion | 进入 = 叠一个 `height=null` 全屏独立层（虚空白背景，被 `dream` 白 token）；槽位 = 青色描边 + 悬停发光；拖素材进槽 = `dnd-kit`；警官反馈 = 06 对话框模块（详见运营/05） |
| 真相板（运营/05 §三） | 线索分组 + 空缺剪影 + 集齐点亮 | `zustand` + `AnimatePresence` | 板块按线索 key 派生分组；未学 = 模糊剪影（`filter: blur`）；集齐 = 板块 `animate` 点亮 + 蓝/青高光 |

---

## 六、各落点的素材需求清单（哪些真需要素材管线、哪些纯代码）

响应 08 §1.2"极少素材"原则，本表明确：**凡是能代码画出的动态一律代码，素材管线只在"静态精密细节"补位**：

| 落点 | 代码动态 | 是否需要素材管线（sprite-forge / asset-pipeline） |
|---|---|---|
| 镜头 lerp / 缩放 / 抖动 | 全部代码 | ❌ 纯代码 |
| 声音混音 / 衰减 / 音效 | `howler` 播放，音效文件来自外包/AI 生成 | ⚠️ 只需音效文件（`03` §11 音效清单），非像素素材 |
| 图层透明度 / 高亮 / 跨层动画 | 全部代码 | ❌ 纯代码 |
| 对话框 / RPG 模块 / 驻地导航 | 全部代码 + tokens | ❌ 纯代码（对话框立绘可能用角色贴图，见下） |
| 出租屋承载物（床/匹配机/书/保险箱/桌） | hover 高光 + 隐藏态 | ✅ **需要静态组件帧**（`sprite-component.py`，map 语境俯视平面视图，`05` 规范） |
| VR 取证室（白虚空 + 桌 + 警官 + 槽位） | 槽位/虚线/发光代码；桌/警官/VR | ✅ **桌、警官需要静态帧**（警官 = 角色管线，桌 = 组件管线） |
| 玩家/NPC/怪物（对局实体） | ORCA 位移 + 姿态曲线 | ✅ 角色管线（`sprite-forge` 母版系统） |
| 底图（梦境场景） | 简笔画背景渲染 | ✅ 底图管线（asset-pipeline / Nano Banana，`02_地图生产管线`） |

> **分界铁律**：素材只在"**零渲染继承不到**的静态精密细节"补位（`00` §二·六）：可交互/有生命的用代码动态 + tokens 语义色；静态承载物与角色用素材管线产出；**任何可以靠曲线/透明度/颜色语义表达的都优先代码**，不因"想省事"就请素材。

---

## 七、落地顺序建议（三命令门禁可验证的里程碑）

不一次铺完，按"先让玩法可读 → 再提质感 → 补氛围"的次序（承接 `03` §Ⅻ + `00` §二·五）：

1. **镜头最小集**：对局焦点跟随（lerp + 缩放）+ 关键演出机位 + 屏幕抖动可关——保证"镜头聚焦该看谁"（`02` §一/§二/§四）。
2. **声音最小集**：四层混音 + 关键反馈音效 + 距离衰减——保证"换弹=音效+曲线"的省力杠杆先落地（`03` §二/§三/§四）。
3. **图层最小集**：透明度函数 + 焦点判定 + `Δh≤9` 高亮——保证"叠层分离 + 跨层可交互"（`04` 核心）。
4. **UI 最小集**：对话框（P5 动态）+ 通知 + 轮次栏（08 已钉）——保证叙事与对局反馈可读。
5. **驻地与局外**：出租屋承载物高亮 + 纯白显形 + 保险箱/书架/证据提交层——整个局外体验串起来。

**每个里程碑都以 `08` 的 2.4 映射表 + 本文件的对应小节为唯一落点**，做完过三命令门禁，新效果未登记不实现。

---

## 五·补、创作工具 / 地图编辑器 / 玩法包 UI → 库落点（承接 `01` 尚待细化剩项）

> `01`「创作工具表现接入」标记这些界面的**画法为待细化、不画线框、只预留端口**。本节按 `01` 已定的交互语义 + `09` 总原则钉到库，**仍不画线框**（线框归编辑器/工作台的专项实现），但把每个"交互语义"落到一个可实现的组件 + 库。这些界面**完全独立切换屏幕、不与战斗内 UI 共享布局**（01 §创作工具的 UI 切换），因此不复用轮次栏/动作菜单，只消费 `UiDiagnostic` / `MapDiagnostic` 统一管线。

| 交互语义（01 出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 创作工具完全独立切换屏幕（01 仓库/地图编辑器/工作台/LLM） | 应用级路由切换 + 独立 layout | `zustand`（当前工具）+ 页面级 `AnimatePresence` | `toolStore.activeTool: 'repo'\|'map'\|'bench'\|'llm'`；每工具一个独立顶部容器，切换用 `AnimatePresence mode="wait"`，**不复用对局 HUD 组件**；共用 `UiDiagnostic` 消费端 |
| 驻地带载物命名映射（01 床/研究台/电脑/书架/保险箱 → 各工具入口） | UI 切换时的命名映射 | `zustand` + tokens | 床=装载入口、研究台=工作台、电脑=地图编辑器、书架=梦境素材库、保险箱=收藏室；**职责两两不可混同**；入口高亮复用 §五 出租屋承载物行 |
| 地图编辑器诊断列表（01 21 条 `MAP_*`） | 左侧面板按 severity 分组 + 点击定位 + 高亮 + correction | zustand + Radix（可折叠分组）+ 镜头 fly（复用 §一 编辑器诊断机位） | `diagStore` 消费 `MapDiagnostic[]`；左面板 `error`/`warning`/`info` 分组；点击 = 复用镜头 §一"编辑器诊断飞行"（`cameraStore.preset` + Framer Motion `animate` 飞过去）；命中元素红色/黄色边缘闪烁（01 高亮语义）；右侧 `correction` 悬浮提示 = Radix `tooltip` 常驻 |
| 地图编辑器人机协作三图元（01 遮挡框/过渡窗口/语义锚点框） | 拖框/划线/放窗口 → 逻辑严格声明 | SVG 画布交互（`pointer` 事件）+ zustand + `dnd-kit`（拖拽） | 图元兜底 = 原生 SVG（几何标注不属 UI 动效，08 六条硬纪律的 Framer Motion 只管动效不管几何编辑）；拖框/划线存 `zustand` 编辑态，落盘译成视觉/物理遮挡（D-074），**AI 读编码不看图** |
| 玩法包拖拽顺序列表（D-073，后装覆盖先装） | 可拖拽不报错的顺序列表，从上到下 = 装载先后 | `dnd-kit`（拖拽排序，08 后置档）+ zustand | 列表项 = 玩法包卡（青色 UGC 角标）；拖拽上下 = 改覆盖优先级；**任何拖拽都合法**，最终态 = 剩余包按新顺序整体重装载（D-073）；移除包即触发重装载；不引入"拔出/插拔"心智——玩家看到的只有顺序化的最终玩法层 |
| 玩法包角标（D-072 ∕ D-076 ∕ D-077） | UGC 角标（青）+ 高级角标叠加，不新增主色 | lucide 角标 + tokens + Radix `tooltip` | UGC = 青色来源角标；高级 = 含地图/引引擎层的徽标，**只在自建房/单机上下文出现**，探索匹配不展示高级包；角标只作来源/地位标识，不参与编译判定 |
| 上传玩法包两态口子（D-076 ∕ D-077 ∕ D-079） | 一态=可插拔普通包（带 UGC 角标）；二态=带图包按地图切多口 | zustand + 选图界面卡片 + 分入口路由 | 带图上传包按地图生成隔离口子，每口进的都是整个上传包、禁从其他口入（D-079）；游戏内只支持建地图，不支持建"地图玩法包"（地图+身上素材整体算玩法包） |
| 背包槽位拖拽 + 掉落的落点预览/取消区（01 输入等价/平板端） | 拖拽交换网格 + 不合法落点回原位说明 | `dnd-kit`（拖拽）+ zustand + Radix `tooltip` | 已在 §三·五 D 物品栏行；拖拽不合法回原位 + 说明原因；触控等同义（长按拖 + 落点预览），目标间距不缩小 |
| 快捷素材栏 7 格→70 格矩阵 + 拖素材画图元（创作/01 图元笔自动放下） | 展开动画 + 拖拽工具 | `dnd-kit` + Framer Motion 展开 | 已覆盖 §五 快捷素材栏行；拖素材切放置动作（画图元笔自动放下，见 创作/01） |

> **创作工具纪律**：编辑器画布是**几何编辑**，几何操作/拖拽用 SVG + `dnd-kit`，**Framer Motion 只管 UI 微动效、tsparticles 不进场**（编辑器不是战斗场景）；诊断配色走 tokens 语义色（红=致命/黄=警戒，01 视觉定律 1），不新增主色。

---

## 五·补·二、AI 调试叠加层（承接 `L2/07` §AI调试工具，与 01 对局 HUD 分离）

> `L2/07_AI系统.md` §AI调试工具 给出了 `AIDebugger` 接口（搜索树可视化 / 估值热力图 / NPC 状态机追踪 / 感知范围显示）与 `AIProfiler`（性能监控）。**这些是开发期调试工具，不是对局玩家 UI**——与 §三·五·补「AI 行为可视化」的评判一致：图上的是内部决策过程，绝不进玩家对局 HUD（违反 01 低心智性）。只在开发期 debug 面板可选开，且感知/隐藏策略不泄露给对手。

| 设计参数（L2/07 出处） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| 搜索树可视化 `visualizeSearchTree` | 树/图拓扑绘制 + 展开收缩 | 原生 SVG + zustand（选中节点）+ `dnd-kit`（pan） | 开发期 overlay；SVG 画搜索树，节点高亮走 tokens 语义色；不属对局 UI，默认全关 |
| 估值函数热力图 `heatmapEvaluation` | 数值 → 颜色映射网格 | CSS 图层 + 纯 TS 颜色函数 | 对地图节点叠加半透明热力色带（`saturate` + 色相映射，红/蓝极化）；只在 debug 面板勾选时显示 |
| NPC 状态机追踪 `traceStateMachine` | 状态日志 + 时间线 | Radix `tabs` + 原生列表 + Framer Motion（reorder） | debug 面板按 NPC 分组，状态变化 timeline；可暂停高亮当前状态 |
| 感知范围显示 `showPerceptionRange` | 听觉/视觉/嗅觉半径圈 | 原生 SVG 圆 + tokens | 叠加在 NPC 周围（听觉/视觉/嗅觉用不同虚线圆/颜色）；**这是开发期辅助，不在对局 HUD 常驻**——对局内感知类线索看 01 §移动模式黄光/警戒语义，不画 AI 半径圈 |
| 性能监控 `AIProfiler`（depth/节点数/剪枝率/分支因子） | 指标数值 + 波动 | zustand + 小浮动条 | 开发期浮窗；数值是内部实现度量，不进玩家可见数值（数值铁律只管玩家可见的 1–5，调试指标例外） |

> **调试层纪律**：AI 调试叠加层只服务开发者调参，**与对局 HUD 是两套渲染、可整体开关**。任何时候玩家对局画面不得出现搜索树/热力图/感知半径等 AI 内部视图（违反低心智 + 信息对称：对局内 AI 的"看/听"由玩法层投影决定，表现层不画 AI 认知图）。

---

## 五·补·三、地图编辑器诊断的数据同构（承接 `L2/06` §5.9 + `01` 地图编辑器诊断）

`01` §地图编辑器诊断呈现 已把 21 条 `MAP_*` 诊断的交互钉在 §五·补（本文件）：左侧面板按 severity 分组、点击定位、红/黄闪烁、correction 悬浮。这里补 **数据来源**：编辑器诊断消费的正是 `L2/06` §5.9 的 `UGCDiagnostic` 同构（`code/severity/path/message/correction/sourceSpan/relatedPaths`）——**一律走 `UiDiagnostic` 统一管线，编辑器与战斗内 `PlaypackDiagnostic` 共用验证管线**（01 已声明二者同构）。

| 诊断管道（L2/06 §5.9 + 01） | 需要的实现 | 指定工具 | AI 落点 |
|---|---|---|---|
| `UGCDiagnostic` 同构消费 | 编辑器渲染 `MapDiagnostic[]`（`MAP_*`），战斗/编译渲染 `PlaypackDiagnostic` | 一个 `diagStore` + 通用诊断组件 | 编辑器用 `MAP_*`，编译/玩法用 `Playpack*`，**共用 `<DiagnosticPanel severity-grouped>` 组件 + tokens severity 色**；`correction` 服务创作者、`message` 亦可读（L2/06：两者缺一不可） |
| severity 语义色 | error=红/warning=黄/info=中性 | tokens（视觉定律 1） | 列表分组 + 元素高亮都用同一色板：error 红边闪烁、warning 黄边闪烁、info 无闪烁；不新增色 |
| sourceSpan 定位 | 诊断点 → 地图元素跳转 | `diagStore.selectedCode` + 镜头 fly（复用 §一 编辑器诊断机位） | 点击诊断根据 `sourceSpan`/`relatedPaths` 定位节点/连接/放置，摄像机飞去高亮；`correction` 右侧悬浮 |

> **编辑器诊断纪律**：编辑器诊断不参与玩法结算（`01` 已声明"不是战斗内 UI，不使用轮次栏/动作菜单"），只是把基底层的验证结果翻译成可定位、可修正的创作者反馈。色调/图标全走 tokens + lucide，不新增独立诊断主色。

---

## 八、08「可选 / 后置」库的落地判定（dnd-kit / gsap / three / pixi / xstate / canvas-confetti）

> 08 §2.3 的后置/可选档写了"引入条件"，本表把每个库在已见设计里的**具体触发点**钉死：哪个设计场景会让它从后置转正。**没有触发点就走 09 现有库（zustand/Framer Motion/tsparticles/lucide/Radix），不提前装。**

| 库 | 08 档位 | 已见设计里的**转正触发点** | 落地判定（截至本文件） |
|---|---|---|---|
| `dnd-kit` | 后置（编辑器二期） | **玩法包拖拽顺序列表（D-073）**、地图编辑器三图元拖放、背包槽位拖拽、快捷素材栏拖素材（本文件 §五·补） | ✅ **已触发转正** —— 拖拽交互是玩法包装载 + 编辑器 + 背包的核心输入，无法用现有库表达；第 08 后置档可摘到必装（需玩家确认，见 08 纪律 6） |
| `gsap` | 后置（Framer Motion 表达不了时） | 复杂 time 轴 multi-segment 演出序列（多段入场/交错）超出 Framer `useAnimate`/variants | ⚠️ 暂不转正 —— 全屏仪式动画（CeremonialTimeline）目前用 Framer `animate` 序列可表达（01 §动画编排），无实测触发点；若 M2 对局出现多轨时间轴编排才装 |
| `three` / `@react-three/fiber` | 后置（3D 镜头/全屏 shader） | 3D 需求或全屏后处理（如 volumetric 光） | ❌ 不转正 —— 全游戏锁定**俯视平面视图 2D**（01 §视觉风格定位），无 3D 视角；全屏演出用 2D 蒙版/粒子即可 |
| `pixi.js` | 后置（2D 高性能批量渲染） | SVG 同屏 DOM 节点成性能瓶颈（大量实体/粒子时） | ⚠️ 暂不转正 —— 目前 DOM 节点量可控；只有实测到卡顿才评估，未到瓶颈不装 |
| `@xstate/xstate` | 后置（复杂 UI 状态机 >3 层嵌套） | 表现层出现 >3 层嵌套状态机 | ⚠️ 暂不转正 —— 现所有 UI 状态（镜头/图层/对话框/动作菜单）都用 zustand 手写且 ≤3 层；只有出现真正 4+ 层嵌套才装 |
| `canvas-confetti` | 可选（比 tsparticles 轻的庆祝） | 成就/任务完成的一性次彩带 | ⚠️ 可用可不用 —— 轻量庆祝可 `canvas-confetti`，重粒子氛围仍走 tsparticles；**撞 08 纪律 6：需玩家裁决是否进必装档** |

> **判定纪律**：以上判定是**截至本文件的快照**，写的是"哪个设计会触发转正"不是"现在必须装"。唯一已实锤转正义务的是 `dnd-kit`（有真实且无法绕过的设计触发点）；其余保持后置，等 FR 触发点出现再装。**任何新库进必装档，按 08 纪律 6 需项目所有者裁决，文档不改、代码不许偷偷引入。**

---
