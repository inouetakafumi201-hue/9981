# 主状态板（Master Status Board）

> **用途**：全项目**唯一进度真相源**。任何时候想知道"现在到哪了 / 什么能并行 / 什么被什么卡住"，只看这一页。
> **权威层级**：L0.5（低于 `docs/L0_规范宪法.md` 与 `docs/访谈决策记录.md`，高于各 Spec 的 `tasks.md`）。
> **维护规则**：本页由主控会话在每轮实质进度后更新；并行子会话**不直接改本页**，通过"交接项/裁决入口"回流（见 `docs/00_并行作战手册.md`）。
> **最后更新**：2026-08-15（M10 状态机端到端靶真实跑通 + 并列专项收束入账 + verify:docs 归档清源；本页从 08-13 到 08-15 的并列产出已补进下方各节）

---

## 〇、为什么需要这一页（读一次就懂当前困境）

各 Spec 的 `tasks.md` 复选框**已与现实严重脱节**，无法作为进度信号：

| Spec | `tasks.md` 复选框自称 | 代码/测试现实 | 结论 |
|---|---|---|---|
| meta-mechanism-kernel | **0 done / 151 todo** | 引擎层功能完整、2010 测试全绿、13 层审查完成 | 复选框从未维护，**严重低估** |
| wakeup-ai | **13 done / 0 todo** | 未独立复核 | 复选框声称全完，**需实测验证** |
| wakeup-core-mechanics | 0 done / 79 todo | playpack 核心机制全绿 | 复选框未维护，**严重低估** |

**因此**：本页以 **①测试套件、②typecheck、③lint、④已确认裁决** 为进度真相，**不以复选框为准**。把复选框与现实对齐已列为并行任务 **PT-03**。

---

## 一、仓库健康快照（每次实质改动后重跑三条命令刷新）

| 指标 | 命令 | 覆盖范围 | 结果（**2026-08-15 复测**） |
|---|---|---|---|
| 类型检查 | `npm run typecheck`（= `tsc --noEmit`） | **`src` + `test` 全域** | ✅ **0 error**（2026-08-16 复测；含 registry-bridge 装配点；`src/play/core-mechanics/defs/rules.match.ts` 3 处为未跟踪并行线产物，非本线） |
| 测试 | `npx vitest run`（= `npm test`） | `src/**`、`test/l2/**`、`test/properties/**`、`test/toolchain/**` | ✅ **3254 passed / 350 文件**（全量全绿；此前 08-13 的 11 失败与 08-15 收束批的 combat-first 阶段2 红测均已归零） |
| Lint | `npm run lint`（= `eslint src test --ext .ts,.tsx`） | **`src` + `test` 两棵树** | ✅ 0 error（147 warning 均为既存未使用符号类，非阻塞） |
| 数据门禁 | `npm run verify:data` | profiles 52 + class 38 共 90 份 JSON 严格可解析 | ✅ 全部通过 |
| 文档门禁 | `npm run verify:docs` | 宪法铁律/废用词/术语一致性/来源追踪 | ✅ **全部校验通过**（2026-08-15 归档 7 个一次性表现系统报告 + 修正 3 处 `Among-Us` 式第三方命名后转绿） |
| L2 隔离门禁（补充） | `npm run typecheck:l2` | `src/l2` + `test/l2` | ✅ 0 error |
| 开发板门禁（补充） | `npm run typecheck:devboard` / `devboard:build` | `src/devboard` | ✅ 0 error；`vite build` 产出 `dist/`，`devboard` 可启动 localhost:5173 |
| 一键全量 | `npm run verify` | typecheck → lint → test → data → docs | ✅ **全绿**（补记：combat-first 阶段2 红测此前归因 AI 并行轮子，AI 线收敛后已归零） |
| Lint | `npm run lint`（= `eslint src test --ext .ts,.tsx`） | **`src` + `test` 两棵树** | ✅ 0 error / ⚠️ **8 warning**（2026-08-10 重测，均为既有未使用符号交接项） |
| L2 隔离门禁（补充） | `npm run typecheck:l2` | `src/l2` + `test/l2` + 其显式依赖的 2 个引擎层 Op 入口（`tsconfig.l2.json`） | ✅ **0 error**（证明基类层可独立通过类型检查；**不替代**上面的全域 typecheck） |
| 门禁范围与文档纪律守卫 | `npx vitest run test/toolchain` | 工具链范围 + 活跃规范文档术语 + 来源追踪采纳状态 | ✅ **2 文件 / 20 断言全过**：12 条工具链范围不变量；8 条文档纪律不变量 |
| 一键全量 | `npm run verify` | 依次跑 typecheck → lint → test | **收束批次（2026-08-13）**：typecheck ✅ 0 error；lint ✅ 0 error / ⚠️ 113 warning；test ⚠️ **2997 测试中 11 失败（2986 通过），4 文件失败**。失败集中在 `test/toolchain/spec-document-discipline.test.ts`（文档纪律 T1，守卫基线过期）与 **另外 3 个数据一致性/玩法测试文件**（`src/class/__tests__/formal-data-integrity.ts`、`src/play/__tests__/capability-binding.ts`、`src/play/__tests__/profile-composition.ts`、`src/play/action-turn/__tests__/action-turn-playpack.ts` 涉及部分，均为既有 data-parity 类失败，属代码/数据线，非本期白名单改动引起）。完整清单见下方「收束批次红灯」。注：初次 `verify` 串联出现过 16 失败瞬态，源于 worktree 并发写半状态，稳定重测回落至 11 失败，以稳定工况为准（PT-06 纪律：并发半写状态不判回归） |

#### 收束批次红灯（2026-08-13 全量 verify，真相源如实登记）

> **口径**：本批次只做白名单真相源同步，**不改任何代码/数据**（disciplined，不改 `src/**` 与 `.kiro/specs/**`）。因此文档纪律守卫暴露的两类绿灯以下红灯，本批次**不越权代修**，仅登记归属、留交接项。

| 红灯 | 内容 | 归属 | 处置 |
|---|---|---|---|
| T1 声明位置契约 7 条 | `.kiro/specs/l2-base-layer-spec/tasks.md` 3 处位置锚点消失 + `docs/访谈决策记录.md` 2 处基线 1→实测 0 + 2 处位置锚点消失 | 文档纪律守卫的声明文件（`spec-document-discipline.test.ts` 的 baseline）与修订过的文档发生位置/条数漂移 | **登记交接项**：需由声明文件的维护线同步收紧位置契约（被修正的合法语义行）与下调已清零基线；非本批次白名单范围 |
| T1 已知违规基线 5 条 | `docs/L_归档/12_根基完备性审查_v2_历史.md` 基线 4→实测 0、`docs/L_归档/AI完备性与文档对齐分析_历史.md` 2→0、`docs/L1_引擎层/05_底层引擎架构.md` 2→0、`docs/L_归档/审查状态综合报告_历史.md` 13→0、`docs/工程治理/02_技术栈与开发流程.md` 1→0 | 这些文档的旧层名违规已由 Wave 3 术语迁移**清理为 0**，但 `KNOWN_VIOLATIONS` 基线条数未同步下调 | **合规现象（好事）**：术语纪律生效、已有 5 份文档清零。需声明文件维护线把已清零文件从 `KNOWN_VIOLATIONS` 移除/下调基线，使守卫回到全绿。属 `test/toolchain/` 维护线，非本批次白名单（前三份与前四份均已随 2026-08-15 文档清理迁入 `L_归档`） |
| 数据一致性 4 文件 / 另 8 条 | `formal-data-integrity.ts`（status/profile 一一对应 2 条）、`capability-binding.ts`（散射/扫射/连发 target_limit 3 条）、`profile-composition.ts`（状态/载具引用 6 条）、`action-turn-playpack.ts`（逆转 AP/SP 2 条） | 既有 **data-parity** 类失败（weapon/NPC/vehicle 能力绑定与 profile 组合，见记忆「Weapon/NPC data parity handoff」：仅 vehicle vehicleParameters + 陈旧 scatter/sweep/burst-attribute-id 测试未清） | 属代码/数据线，非本期白名单改动引起；按并行纪律登记交接，由相应数据线收敛（Wave 4 相关面） |

> **复盘**：本批次白名单改动只加了 `FROZEN.md`（新增，无废用词）并刷新锁/索引/状态板（均不含废用复合词命中）。verify 的 11 失败分两类：①文档纪律守卫 T1（baseline 未随 Wave 3 清理同步，共 T1 各测试）；②既有的 `data-parity` 数据一致性失败（status/capability/profile/vehicle 组合相关 8 条）——两者**均非本期改动引起**，且本批次受白名单约束不改代码/数据，故如实登记、留交接、不回填。稳定重测（PT-06「并发半写不判回归」纪律）确认失败数为 11 而非首轮瞬态 16。

#### 此前红灯归属记录（已转绿，留档供参照并行纪律）

| 曾失败测试 | 归属 | 后续 |
|---|---|---|
| `src/class/__tests__/scene-catalog-activation.property.test.ts`（曾 2 条） | L2/class 线（PT-01 相关：scene-catalog-activation 桥接） | ✅ 已转绿，4/4 通过；已提交（commit `e375a6e`） |
| `src/ui/model/__tests__/profile.test.ts`（曾 1 条） | PT-09（UI） | ✅ 已转绿，10/10 通过；`profile.ts` 已按测试期望的完整契约重写（含 `authoritativeSource` 逐段登记 + 决策记录交叉核对）并提交 |
| `ugc-friendliness.test.ts`（1 条）+ `i18n-readiness.test.ts`（2 条） | spec-compiler（E 范围，`integrity.ts` 作者在场） | ✅ 已转绿，34/34 通过。**根因**：`findSemanticFieldDamage` 把"表现字段按 schema 登记的回退值降级"误判为语义丢失，转成 infrastructure 停机而非受制裁的 candidate 拒绝警告；`integrity.ts` 自身文档注释早已写明此豁免，`compareOneDefinition` 补上 `rule.presentation===true && 值等于 rule.fallback` 的判断后即修复。由该文件的活跃作者本人落地，未被外部会话越权代改（见下方交接记录） |

> 结论验证：文档纪律改动落地时曾达 **198 文件 / 2292 测试全绿**；随后并发线把套件扩到 204/2337，最新全量出现 2 个新失败，UI 项已自行恢复，UGC 零耦合守卫仍红。**当前仓库不能宣称全绿**；该红灯属于新 UGC 跨 Spec 装配尝试，不由文档守卫线越权修复。

#### 交接记录：`findSemanticFieldDamage` 表现字段豁免（2026-08-10）

一个并行会话精确定位了此问题（4 处 `canonicalSnapshot` 接线 + 此处根因），**主动不越权代改**——因为 `integrity.ts` 是本会话期间才被另一并行工作创建、作者在场，遂只做定位并登记交接项。主控会话复核确认修复已由该文件的作者线落地（`rule.presentation===true` 豁免已在 `compareOneDefinition` 中实现），无需二次动手，验证通过后直接提交。
**协作纪律范例**：这是"发现职责重复/边界，先判断该不该自己动手，不确定就交接而不抢"的正确示范，记录在此供后续参照。

> **PT-06 已闭合此前的检查盲区**。原 `tsconfig.json` 的 `include` 是 `["src", "test/properties"]`，**`test/l2` 不在 typecheck 范围**；`tsconfig.l2.json` 虽含 `test/l2` 但没有 npm 脚本会跑它；`npm run lint` 也只 `eslint src`。
>
> **Bug 记录（供避雷，问题已根治但教训保留）**：因上述盲区，"测试引用一个已从类型上删除的字段"这类错误 **`tsc` 抓不到**，只在运行期暴露为 `undefined`。`attackShape` 于 2026-08-08 从 `ActionDescriptor` 删除，但 `test/l2/integration/end-to-end.integration.test.ts` 的断言未同步移除，遂在全量运行时才失败。**成因不是"忘了改测试"，而是"改了也没有门禁会告诉你"**——所以根治手段必须是扩大检查范围，而不是补一次人工排查。
> **门禁有效性已实证**（不是"退出码 0 所以以为覆盖到了"）：在 `test/l2/` 下放入探针文件引用 `Diagnostic.attackShape`（不存在的字段），新范围 `tsc` 报 `TS2339` 并以退出码 2 失败；同一文件在旧范围（`["src","test/properties"]`）下 `tsc` 退出码 0 —— 盲区与修复均得到对照验证。探针文件验证后已删除。
>
> 若三条命令任一出现漂移，**先重跑再判断**，不要沿用旧快照；注意 vitest 会缓存 `node_modules/.vite/vitest/results.json`，改了被依赖的模型后应全量重跑而非信任缓存（历史教训：仓库曾被并发会话高频改动导致基线反复变化）。
>
> **⚠️ PT-06 生效带来的并行纪律要求（实测踩到过，必须知道）**：typecheck 范围扩大到 `test` 全域并且 `src` 下新目录也一并纳入后，**任何一条线处于半写状态的文件都会让全仓 `typecheck` 变红**。
> 2026-08-09 深夜就出现过这种情况：`src/l2/ugc/ports/diagnostic-projection.ts` 报 `TS1005 '}' expected`、`src/ui/model/diagnostic.ts` 报 `TS2307 Cannot find module './revision.js'` —— 两者都只是"写到一半"，几十分钟后两条线各自写完，全仓 typecheck 自行恢复 0 error。
> **因此判定"三条命令全绿"必须在各并行线回流完成后进行**；在别人写到一半时抽查并宣布"出现回归"是误判。当需要在并发中确认自己那部分是否干净，用 `extends` 主 tsconfig 并 `exclude` 掉他人在写目录的临时配置来隔离验证（用完删除）。

### 一之补：PT-06 纳入检查后暴露的交接项（8 条 lint warning，均**未**在 PT-06 内自行修改）

> 理由：PT-06 的黑名单禁止改 `src/**`、`test/**` 的代码内容；这些文件分属其他线的交付物，按"不跨 Spec 改别人的交付物"登记为交接项。**全部是"声明了但未使用"，无行为影响，不阻塞任何门禁**（`no-unused-vars` 配置为 `warn`，允许 `_` 前缀豁免）。
>
> **口径说明（含一次已闭合的插曲）**：下表是 **PT-06 完成时刻（2026-08-09）** 的快照。2026-08-10 期间 lint 曾涨到 43 warning，新增 35 条来自 PT-02（`src/l2/ugc/ports/`）与 PT-09（`src/ui/`）本轮新写的代码；当时的处置是**不搬进本表**（否则这张表会变成替别人记账，而不是 PT-06 的交接清单），只登记归属。**结果印证了这一处置**：两条线各自清理后，01:40 重测已回落到 8 条，恰好就是下表这 8 条，一条不多。

| # | 文件 | 位置 | 未使用符号 | 归属线 |
|---|---|---|---|---|
| 1 | `src/play/core-mechanics/load.ts` | 55:3 | `PHASE_SETTLE` | L3 / PT-07 |
| 2 | `src/play/core-mechanics/projection.ts` | 12:15 | `Diagnostic` | L3 / PT-07 |
| 3 | `src/play/profiles/audit.ts` | 137:10 | `checkMembers` | L3 / PT-07 |
| 4 | `test/l2/integration/adapter-consumers.integration.test.ts` | 10:70 | `validActionContract` | L2 / PT-01 |
| 5 | `test/l2/integration/end-to-end.integration.test.ts` | 14:10 | `FaultInjectableKernel` | L2 / PT-01 |
| 6 | `test/l2/properties/family-boundary.property.test.ts` | 9:10 | `DIAGNOSTIC_CODES` | L2 / PT-01 |
| 7 | `test/properties/P04-inheritance-resolution-idempotence.property.test.ts` | 27:10 | `isErrorDiagnostic` | L2 / PT-01 |
| 8 | `test/properties/P11-readonly-projection.property.test.ts` | 41:29 | `ok` | L2 / PT-01 |

> **未做的收紧（如实标注，留给后续裁决）**：没有给 lint 加 `--max-warnings 0`。加了会立刻因上表 8 条而红，而修它们要动别人的目录——属跨线改动。**建议**：上表清空后，再把 `--max-warnings 0` 加进 `lint` 脚本，让 lint 门禁从"软"变"硬"。在那之前，`npm run lint` 只对 **error** 硬门禁。

### 一之补二：门禁范围守卫 `test/toolchain/gate-coverage.test.ts`（让"已修复"变成"不会再坏"）

> **为什么必须有这个文件**：配置盲区的本质是**没有任何东西在监视范围本身**。只把 `include` 改对、再在文档里写一句"已修复"，下一次有人为了跑得快把 `include` 收窄、或新建一个 `test/<新目录>`，盲区就无声回来了。守卫把"范围"本身变成断言。
> 依据 `AGENTS.md` 结尾「项目实践原则」架构决策原则「契约要机器可校验」「零耦合要有守卫」。

| 编号 | 守住的不变量 | 实现要点 |
|---|---|---|
| **G1** | 磁盘上 `src/`、`test/` 下每个 `.ts`/`.tsx` **都在** typecheck 的 program 里 | 用 `ts.readConfigFile` + `ts.parseJsonConfigFileContent`（TypeScript 自己的解析器）算 `fileNames`，**不自己实现 glob 语义**——否则守卫与真实 `tsc` 会产生偏差，守卫本身就成了新的信任缺口 |
| **G2** | `vitest.config.ts` 每个 include glob 的静态根都落在被检查的树里 | 取首个含通配符的段之前的部分作为根；新增 `e2e/**` 之类会立刻失败，迫使同步扩大 typecheck |
| **G3** | `typecheck` 脚本不得用 `-p`/`--project` 换成窄配置；`typecheck:l2` 仍存在且仍指向 `tsconfig.l2.json`；且 L2 范围**严格小于**全域范围 | 后两条防止"补充门禁"再次退化成没人跑的死配置，或悄悄膨胀成假的全域 |
| **G4** | `lint` 目标含 `src` 与 `test`；磁盘上出现 `.tsx` 时 `--ext` 必须含 `.tsx` | 解析 `package.json` 的 lint 脚本参数（正确跳过 `--ext` 等带值选项） |
| **G5** | lint 对 `test/` **真的生效** | 断言行为而非配置字符串：ESLint 不忽略 `test/` 各顶层子树的代表文件，且在 `test/` 路径下 lint 一段重复键代码会真的报出 `no-dupe-keys` error；另静态排除 `.eslintignore` / `ignorePatterns` 的出现 |
| **G6** | `verify` 串联 typecheck → lint → test | 顺序也断言（typecheck 在 lint 之前） |

> **守卫本身经过反向验证**（否则它可能是一组永真断言）：把 `tsconfig.json` 的 include 改回 `["src","test/properties"]`、`lint` 改回 `eslint src --ext .ts` 后重跑，**恰好且只有 G1、G4 两条失败**；恢复后 12 条全过。
> **性能取舍（自主判断，如实标注）**：G5 最初对 `test/` 下全部 38 个文件调用 `ESLint#isPathIgnored`，实测 42 秒——会把守卫拖成没人愿意跑的慢测试。改为"每个顶层子目录取一个代表文件"后 5 秒内跑完。理由是忽略规则的粒度本来就是目录；更细粒度的漏检由"不存在 `.eslintignore` / `ignorePatterns`"这条静态断言兜住。这是我做的取舍，不是 Prompt 要求。
> **附带改动**：为让守卫被 `npm test` 跑到，`vitest.config.ts` 的 include 增加了 `'test/toolchain/**/*.test.ts'`（纯新增，未改动既有三条的语义）。

### 一之补三：规范文档纪律守卫 `test/toolchain/spec-document-discipline.test.ts`

> **目的**：把 L0 术语纪律与逐要求来源追踪从人工约定变成机器门禁，同时不跨线改写其他 Spec 文档。

| 编号 | 守住的不变量 | 当前事实 |
|---|---|---|
| **D1** | 活跃 `.kiro/specs/**`、`docs/**` 不得新增无歧义废用复合词 | 明确排除归档目录；声明禁令文件按职责豁免且有死豁免检查；6 个文件共 **23 处既有违规**采用精确条数棘轮，新增会失败、修少后必须下调基线，目标为空表 |
| **D2** | 每个活跃 `.kiro/specs/*/requirements.md` 必须显式登记来源追踪采纳状态 | 自动发现与 adoption map 双向比对；新增 Spec 不能静默漏检 |
| **D3** | fully-adopted 必须逐要求小节恰有一个 footer；not-adopted 必须逐节为零；partially-adopted 精确棘轮 | 当前 7 个活跃 Spec：4 个 fully-adopted、3 个 not-adopted、0 个 partially-adopted；`wakeup-ai` 12/12 逐节通过 |

> **自主设计判断**：文档侧没有硬禁单独的多义词，因为消息文案、Prompt、Vue 与文档结构存在合法语境；只硬禁无歧义复合词。历史档案不作为现行规范扫描。既有违规没有被伪装成通过，而是精确登记为待清零交接项。
> **验证**：单文件 8/8；`test/toolchain` 20/20；`tsc --noEmit` 0 error；lint 0 error / 8 warning。文档守卫落地快照曾为全量 198/2292 全绿；并发新增 UGC/UI 文件后扩展全量出现新红灯，当前剩 UGC 零耦合守卫 1 失败，与本守卫无关但已在健康快照如实登记。

### 一之补四：文档同步（已完成，2026-08-10）

- `docs/L_归档/02_项目全局报告_历史.md` §七 第一条信任缺口已改写为**已闭合**，并写明闭合方式经过对照实证、以及守卫的存在。
- `docs/并行作战/00_索引.md`：PT-06 标为已完成；**并纠正了原先"第一波五条线白名单两两不相交"的错误结论**——`.eslintrc.cjs` 与 `package.json` 是 PT-06 与 PT-09 的共享文件，2026-08-09 确实发生了同文件并发改动（PT-06 加范围注释、PT-09 加 `src/ui/**` 渲染层 override），因落点不同而侥幸未冲突。索引现要求这类共享文件串行或明确分区。

---

## 二、三层进度（以现实为准）

> **2026-08-15 增补**：L1 收官轰炸（wakeup-engine-bombardment：id 计数器非原子推进已按宪法整体修复，12 文件 42 用例全绿）；L1/L2 并列契约单源化（wakeup-base-layer-ecs Composition_Shape 重命名 + wakeup-cas-gap-closure T-CaS-01~04：`auditCapabilityComponentContract` / `component-alignment` / `playLayerOwnedFieldNames` 单一源）；L2 基类层收官轰炸（wakeup-base-layer-bombardment：6 属性测试文件全绿，kernelOps↔OpRegistry 机械闭合 + npcs/weapons 15 个族特有待裁决 Op 名登记 Known）；载体参数承载面（VEHICLE_PARAMETER_BINDING_GAP）与 Q-04 待裁决条目已交 L2 线。

### L1 引擎层（`src/core/kernel/`）— 功能完整，可作为稳定基座
- Entity / Component / Relation / State / Expr / Query / Ops / Journal / Events / Hook / Flow / Actions / Decision / Attachments / Schedule / Random / Knowledge / Persistence / Safety 全部落地。
- Hook 分发**已接线**：`wire-hooks.ts` 的 `wireHooksIntoRegistry` 把 `OpRegistry` 的 before/after 接到真实 `HookDispatcher`（H-001 缺口已闭合，D-059 已确认）。
- 13 层属性测试审查完成；独立验证子项目状态见根目录 `docs/L_归档/KERNEL_VERIFICATION_STATUS_历史.md`（4/7/8/11/12 层已完成四件套验证，1l2/3/5/6/9/10/13 标记待重建）。
- **已关闭（2026-08-13）**：`U-001` 投点随机化（1d6）——原始骰点判为内部瞬时量（D-054），1d6 合法、引擎侧随机流启用与否属玩法层装配，U-001 与 T-004② 一并关闭。

### L2 基类层（`src/l2/` + `src/class/`）— 管线与目录已落地，待收敛
- `src/l2/`：编译/校验/解析/注册/运行时提交全套 + `model/**` 规范模型常量；`test/properties/P01–P14`（14 性质全绿）+ `test/l2/**`（23 测试）覆盖。
- `src/class/`：15 个实例目录（scenes/containers/movement/actions/gateways/attachments/skills/items/weapons/vehicles/damage-types 等）统一富格式落地，经 `catalog-loader` + JSON Schema 校验。
- **未决/待执行**：**D-061**（`src/l2` 与 `spec-compiler` 收敛，AI 裁决待复核）→ 执行为 **PT-01**；UGC 端口交付 → **PT-02**。

- **L3 玩法层（`src/play/`）— 行动轮核心已全绿**
- `action-turn/playpack.json` 纯数据；核心机制**全部通过测试**：AP/体力池、投点排序、强力骰、逆转/超逆转（D-053）、招架（D-053+D-032）、弱点命中链（D-049）、`[失衡]`、**过载（D-055，本轮修复装载配置后全绿）**、清理阶段。
- `core-mechanics/` 装载器：本轮补齐 `overload` 绑定后 typecheck 通过。
- **M10 状态机端到端靶真实跑通（2026-08-15，核心交付）**：`loadCoreMechanics(runtime, config)` 首次被真实调用，走完五阶段（roll→settle→playerAction→npcAction→cleanup→回 roll）；官方测试 config（`official-state-machine-config.ts`）翻正 U-001/hookWiring/T-001 装载门禁，装载驱动 `state-machine-load-driver.ts` 复用 `createFullHarness`，e2e 靶 5 绿断言「睡下→起床回满体力 / 攻击打落零血倒地 / 普通倒地经站起移除」一轮内发生；AI 线 `driveMultiTurn` 直接消费组合根补出 M10 靶（`m10-state-machine-consumption.test.ts`+`ai-runtime.ts`）。**过载剔除在 core-mechanics 包内为如实登记的未落差距**（真实过载在 legacy action-turn 包），PLAYER_QUEUE_GAP 亦已登记。
- **已关闭（2026-08-13）**：过载持续时长（L3-DIV-01）已按 `rollsSkipped=1` 对齐；逆转消耗已改 SP（非 AP）；`status_poisoned`/`radiation` 已接「梦境侵蚀度」词条（缩圈由纯白/体力推高表达）；`U-002` 已裁决（单人 2 AP）；`U-001` 已裁决（1d6 内部量）。剩余见第四节 T-001 / L3-DIV 逐条登记。

---

> **2026-08-15 增补 — 地图编辑器（开发板）**：`wakeup-map-editor-devboard` 专项全量落地（commit 63b7661）——`src/devboard` 全套生产级（ports 契约桶 / layer-rules / layer-shapes / workspace-state / map-io / material-availability 全放行 / playtest 接线）+ 3 组测试（core 10 / PBT 5 / e2e 4 = 19 绿）+ 独立构建（`devboard` → localhost:5173，`devboard:build` 出 dist）。**明确留作独立专项（不在 devboard 批）**：①`MapData.floor→layers` 契约扩展（height 可空 + 透明度公式 `opacity=min(1,|Δh|×.1)`，见 `docs/创作系统/01` §九 L.1–L.10）；②素材库元状态层（`material-availability` 钩子目前为"全放行"桩，真实判定独立立项）；③完整编辑器交互工作台（当前三栏工作台已落地，操作面后续专项）。

---

> **2026-08-16 增补 — 装载等价专项（D-081 / L0 第十四条）**：所有玩法规则载入式；官方 TS / UGC JSON 表达手段分级、装载权限不分级——两形态经同一装载契约进注册表、装载后 Def 快照等价（即新玩法层）。权威文档已入账（L0 第十四条 + 访谈决策 D-081 + 06_UGC §2.1/§4.2 + 00_架构域 §1.3）。**三债务**：① `playpack-codec.ts` 拒绝 `play` 治理元数据；② `loadCoreMechanics` 硬编码官方 TS 包、无注入参数；③ `playpack-compiler` 产物无到 PlaypackLoader 的装配桥。**批次规划**：Batch A（A1 codec 放行 / A2 load 参数化 / A3 装配桥 / A4 表达力审计，写权互不相交）；Batch B（B1 双形态端到端等价证明 / B2 UGC 全链路，依赖 A 落地）。不做：不把官方 defs 翻译成 JSON、不开放 JSON 携 linter/可执行代码、不引入「图灵完备」表述。

---

## 三、各 Spec 状态（现实校准版）

| Spec | 层 | 现实状态 | 主要阻塞 / 下一步 |
|---|---|---|---|
| `meta-mechanism-kernel` | L1 | ✅ 功能完整、全绿 | 复选框已对齐现实（PT-03a ✅：125/151 带证据，26 项待测/未做）；无功能阻塞 |
| `l2-base-layer-spec` | L2 | 🟡 管线+目录落地、37 测试绿 | **D-061 执行（PT-01）**；D-1/D-2/D-4/D-5 落点已随 D-061 确定 |
| `wakeup-space-items` | L2 | 🟡 目录大部分已落地、spatial-rules 已实现 | design 任务 9 需按 D-058 改写为"校验既有目录"；复选框已对齐（PT-03b ✅：36 项全 `[ ]`，7 待测[5 个模型文件为死代码]+29 未做[领域层未建]）→ 实施见 PT-08b |
| `wakeup-core-mechanics` | L3 | 🟡 核心机制全绿 | T-001 / U-001 / U-002 待所有者裁决；L3-DIV 数值分歧；复选框已对齐（PT-03c ✅：79 项全 `[ ]`，成因 D-062 冻结 + `core-mechanics/` 目录零测试）→ 收敛见 PT-07 |
| `wakeup-ugc` | UGC | ⚠️ 主链路完整、端口已交付，但最新直接装配尝试触发架构守卫 | PT-02 的 L2 端口存在；然而新建 `src/core/ugc/integration/l2-adapter.ts` 直接导入 `src/l2`，使 `architecture-boundary.test.ts` 的“基类层未冻结时零耦合”用例失败。不得放宽守卫或 force-fit；需先裁定 D-061/端口冻结状态与装配所有权 |
| `wakeup-ai` | L2/AI | 🟡 9 测试文件全绿，但**覆盖率逐条审计后为 44✅/23◐/4⬜**（PT-04 完成） | 存在**零覆盖实现分支**（延后提交 `isDeferred` 分支、宏动作 `SemanticIntent.orderedSteps` 守卫）；要求 10.7/12.5/12.6 的"逐字段登记"无承载物 → 需裁决。清单见 `docs/L_审查报告/wakeup-ai-覆盖率审计.md` §三 |
| `wakeup-ui-animation` | L2/UI 投影 | ✅ **已完成（11/12 tasks）** | 60 文件 / 360 测试全过（含 24 属性测试 numRuns≥100）；typecheck/lint 0 error；变异测试证明不空转。详见 `docs/L_审查报告/PT-09_UI投影完成报告.md` |
| `wakeup-engine-bombardment` | L1 | ✅ **收官轰炸完成（12 文件 42 用例绿）** | id 计数器非原子推进已按宪法整体修复（`state/ids.ts` 作用域 begin/commit/rollback + OpRegistry 顶层事务对齐）；见其 `execution-report.md` |
| `wakeup-engine-layer` | L1 | ✅ **reconciliation 完成** | L2 基类层对账两回完备（metrics 四值 direction / UGC 门单调重定义 / 引擎层目录重写）；载器接线、地图接入、载具参数承载面留交接项。见 `reconciliation-tasks.md` |
| `wakeup-cas-gap-closure` | L3/L2 契约 | ✅ **T-CaS-01~04 已闭环** | `auditCapabilityComponentContract` / `component-alignment` / `playLayerOwnedFieldNames` 单一源归一 + PBT A1-4 全绿。见 deign/requirements/tasks |
| `wakeup-base-layer-ecs` | L1/L2 | ✅ **Composition_Shape 单一源完成** | ECS composition 重命名（registry Template→Shape）、`family-component-shapes` 登记、架构术语门禁恢复全绿 |
| `wakeup-base-layer-bombardment` | L2 | ✅ **收官属性+压力轰炸完成** | 属性 1-13 六测试文件全绿 + kernelOps↔OpRegistry 机械闭合升格；npcs/weapons 15 个族特有待裁决 Op 名登记 Known。见 `execution-report.md` |
| `wakeup-map-editor-devboard` | 开发板 | 🟢 **专项全量落地（commit 63b7661）** | 独立 Vite+React+TS 地图编辑器 + 三步门禁全绿；MapData 契约扩展 / 素材元状态 / 编辑器交互工作台留独立专项 |
| `l2-registry-bridge（专项 D）` | L2 | 🟢 **生产装配点已落地** | `src/l2/kernel/registry-bridge.ts` 用真实 OpRegistry/DefRegistry 产出 KernelContract + 只读 Def 视图，`createKernelContractFromOpRegistry` 生产调用点 0→1；契约测试 8 绿；UI/AI 适配器与 action-submitter 接线留整合层专项 B（04 文档 §2.3 承接链：适配器未接桥是消费方接线，本专项只 export 装配点、登记衔接），见 `docs/工程治理/06_基类层注册表桥_专项prompt.md` |

---

## 四、待项目所有者裁决的开放事项（AI 不代裁 / 需人工确认）

| 编号 | 事项 | 状态 | 来源 |
|---|---|---|---|
| **D-061** | `src/l2` 与 `spec-compiler` 收敛所有权 | ✅ **架构已确认**（2026-08-11 用户明确回复'认可'），需最终批准后可执行 PT-01 | `docs/访谈决策记录.md` D-061 |
| **D-062** | L3 核心机制双实现收敛方向（`action-turn/playpack.json` vs `core-mechanics/*.ts`，含 U-002 矛盾） | ✅ **已收束**（架构方向 2026-08-11 确认 + U-002 2026-08-13 裁决维持 D-037，PT-07 已就绪、Wave 4 已解锁） | `docs/访谈决策记录.md` D-062 |
| **T-001 / Q-01** | 武器谱型"特殊"档机制框架 | ⬜ 待访谈 | L0 §七 Q-01 |
| **U-001** | 投点随机化（1d6）是否启用 vs 维持阻塞 | ✅ **已裁决关闭**（2026-08-13 项目所有者确认 D-054）：原始骰点为内部瞬时量，1d6 合法，启用与否属玩法层装配；U-001 与 T-004② 一并关闭，不再阻塞 | `wakeup-core-mechanics` S7 + `docs/访谈决策记录.md` D-054 |
| **U-002** | 单人投点 AP（D-037 单人 2AP vs D-054 abort） | ✅ **已裁决**（2026-08-13 项目所有者：维持 D-037 单人 2AP，abort 语态作废；PT-07/Wave 4 前置解除，T-004 第②③项关闭） | `docs/访谈决策记录.md` U-002 |
| **L3-DIV-01~11** | 玩法层内部数值分歧（如过载持续时长 3 回合 vs 2 次投点） | ⬜ 逐条登记待裁 | `src/play/profiles/known-divergences.ts` |
| **A-AI-01** | `wakeup-ai` 要求 10.7/12.5/12.6 的"逐字段待汇合契约登记件"**无承载物**，归属层与形状待定 | ⬜ PT-04 提请 | `docs/L_审查报告/00_并行产出裁决与整理.md` §十一 |
| **A-AI-02** | `AICommitOutcome` 的 `'opened-decision'` 在实现中无任何产出点：是待实现还是应移除 | ⬜ PT-04 提请 | 同上 |
| **A-AI-03** | `wakeup-ai` 两处**零覆盖实现分支**（延后提交 `isDeferred` 分支、宏动作 `orderedSteps` 守卫）由哪条线补测 | ⬜ PT-04 提请 | 同上 |

> **2026-08-15 增补（基类层/引擎层收官交 L2 线裁决）**：
> - **Q-04 / 载具参数承载面（VEHICLE_PARAMETER_BINDING_GAP）**：`src/class/vehicles` 的 field-name 槽指向不存在的顶层键，归因基类层槽位指向；裁定「载具参数承载面」后由 L2 线消解，见 `UNRESOLVED_PARAMETER_BINDINGS` 与 wakeup-base-layer-bombardment execution-report。
> - **registerMapAnchor 零消费**：收敛归属待裁决。
> - **引擎层 reconciliation（wakeup-engine-layer + wakeup-engine-bombardment）**：metrics 消费方向四值 / 单调重定义 UGC 门 / 引擎层目录重写已落地，载器接线与地图接入留交接项。

> **原则（D-060）**：以上属"文档矛盾/未决"，正确处置是**提请裁决并登记**，**不得默认否决已批准机制**。

---

## 五、并行任务队列（PT）——完整 Prompt 见 `docs/并行作战/`，方法见 `docs/00_并行作战手册.md`

> 信任与断点依据：`docs/L_归档/02_项目全局报告_历史.md`。**只从 🟢 就绪线派发**，🔒 线等前置裁决。

| ID | 任务 | 就绪状态 | 依赖 |
|---|---|---|---|
| ~~**PT-02**~~ | l2 导出 UGC 消费端口，解除 `wakeup-ugc` task 11.1 | ✅ **已完成**（2026-08-10）：`src/l2/ugc/ports/` 落地三端口（validation/resolution/registries[layer]）+ 诊断投影（全码 `satisfies` 穷举）+ `decodedValue→DefinitionPackage` 映射与覆盖授权 + 封闭Schema/提供方判定/包依赖环补充能力；入口 `createL2PortBundle()` 通过 `inspectL2PortBundle`；不 import `l2-port-contract`（结构对齐无耦合）。`tsc` 0 错、`vitest` 2158 全绿、`eslint` 0 错。交接项见跨Spec契约 §八。 | 无 |
| ~~**PT-03**~~ | 三个 spec 的 `tasks.md` 复选框对齐测试现实（可拆 3 并行） | ✅ **已完成**（2026-08-10）：三个 `tasks.md` 逐条对齐并带可核对注记（每个 `[x]` 有 `（证据：路径）`、每个 `[ ]` 有 `待测/未做` 原因），经机械校验（证据路径全部 `Test-Path` 存在）。结果：meta 125✅/26未（12待测+14未做）；core-mechanics 0✅/79未（D-062 冻结+零测试，全 `[ ]`）；space-items 0✅/36未（5 个模型文件为死代码待测、其余领域层未建）。**PT-03b 子代理曾越界扩写 space-items tasks.md，已回退到 PT-08a 版再最小标注** | 无 |
| ~~**PT-06**~~ | 把 `test/**` 纳入 typecheck+lint，堵"写了不被检查" | ✅ **已完成**（2026-08-09/10）：`tsconfig.json` include→`["src","test"]`；`lint`→`eslint src test --ext .ts,.tsx`；新增 `typecheck:l2`、`verify` 脚本；**并落地机器守卫 `test/toolchain/gate-coverage.test.ts`（12 断言，经反向验证有牙齿）防止范围被静默缩回**。门禁有效性已用探针对照实证。文档同步已完成（§一之补三） | 无 |
| ~~**PT-08a**~~ | 按 D-058 重写 `wakeup-space-items/tasks.md`（校验既有而非新建） | ✅ **已完成**（2026-08-09）：`tasks.md` 全量重写为 A 校验既有 / B 补验证面 / C 补目录字段三类任务，覆盖 requirements 1–14 逐条状态矩阵（✅/◐/⬜/⚠），并发现两项基线事实：①`src/l2/model/space-items-*.ts` 五个领域模型文件**已产出但是死代码**（无导出、无消费者、无测试）；②新增 `tsconfig.l2.json` 隔离门禁未被旧计划提及。留 6 项待裁决（H-01~H-06）、5 项风险（R-01~R-05）、4 项交接（T-01~T-04，含 design.md 过时描述需修订登记为 **PT-08c**）。 | 无 |
| ~~**PT-04**~~ | `wakeup-ai` 覆盖率审计 | ✅ **已完成并回流守卫**（2026-08-10）：71 条验收标准逐条矩阵，44✅/23◐/4⬜；报告 `docs/L_审查报告/wakeup-ai-覆盖率审计.md`。B-19/B-20 已由 `test/toolchain/spec-document-discipline.test.ts` 承载；术语侧仍有 23 处既有违规精确基线，未宣称清零 | 无 |
| **PT-01** | 执行 D-061：退役 `SpecificationCompiler`、缺口迁入 `src/l2`、更名 JSON 原语 | 🟡 **已解锁**（D-061 架构已确认 2026-08-11、PT-02 已完成）；需最终批准后转就绪 | D-061 最终批准 |
| **PT-08b** | 按新版 `wakeup-space-items/tasks.md` 实施：接线死代码、补验证面/契约、补目录字段、跑属性测试 | 🟢 **就绪**（PT-08a 已完成）；Prompt 已产出（`docs/并行作战/PT-08b_space-items实施.md`） | 无 |
| **PT-09** | `wakeup-ui-animation` 从 task 0 起建 `src/ui` 并按序推进 | ✅ **已完成**（Tasks 0-11，360 测试全绿）| 详见 `docs/L_审查报告/PT-09_UI投影完成报告.md` |
| **PT-10** | UGC 真实上游集成：消费 l2 端口（`createL2PortBundle`）、补齐 task 11.1/11.3 | 🟢 **就绪**（PT-02 端口已交付）；Prompt 已产出（`docs/并行作战/PT-10_UGC真实上游集成.md`）；定向 28/28 全绿（架构守卫已校准为"端口消费"，对应 PT-02 冻结事实）；UGC 457/457 全绿；**可继续执行** | PT-02 ✅；详见 `docs/L_审查报告/PT-10_架构守卫校准完成报告.md` |
| **PT-07** | L3 核心机制双实现收敛（D-062）+ 补齐必交付属性测试 | 🟢 **已就绪**（U-002 于 2026-08-13 由项目所有者裁决：维持 D-037 单人投点 2 AP，abort 语态废弃 → Wave 4 解锁）；Prompt 已产出（`docs/并行作战/PT-07_L3核心机制收敛_阻塞.md`） | D-062 批准 ✅ + U-002 裁决 ✅（二者均已满足） |

> **2026-08-15 增补 — 已收束的并列专项（非 PT 编号，已落地入账）**：engine-bombardment（收官轰炸，id 计数器原子性整体修复）、base-layer-ecs（Composition_Shape 单一源）、cas-gap-closure（T-CaS-01~04 缝隙闭合）、base-layer-bombardment（基类层收官属性轰炸）、engine-layer（L2 对账 reconciliation）、devboard（地图编辑器专项）。这些不进 PT 队列，各自 spec 的 `execution-report.md`/tasks 是入账载体。**尚待立项的独立专项**：①MapData `floor→layers` 契约扩展；②素材库元状态层；③整合层/装载运行期——统筹规划已完成，见 `docs/工程治理/04_整合层_装载运行期_规划设计.md`（预立项）：面貌=生产组合根 `createLoadedMatch` + 接线面（UI/地图/对局外壳/domain-entry）；**引用权归玩法层**（owner 定型），专项已拆为 **A 玩法层彻查（CEME，扫玩法层全部未定义需求，覆盖 `outcomes` 空/PLAYER_QUEUE_GAP/参与者资格，整合层硬前置）** 与 **B 整合层本体（组合根+对局外壳+UI宿主）**；已裁 Q-2/Q-3 归玩法层、Q-4 保留测试组合根+装配字节级一致、Q-5 L1↔L2 桥归基类层、Q-6 并入 Q-4，待裁仅 Q-1 模块名。**2026-08-16 状态：A（CEME spec 已冻结，`requirem/design/tasks.md` C-1~C-10）与 C（基类层注册表桥专项 D 已落地，`src/l2/kernel/registry-bridge.ts` 8 绿）均已完成，B 可启动，专项 B prompt 见 `docs/工程治理/07_整合层本体B_专项prompt.md`**。技术=复用 `createFullHarness` 能力子集、`loadCoreMechanics`、`CoreMechanicsFacade`、`createPlayAiRuntime`、`createUiSystem` 7 端口、`PresentationGateway.subscribe('*')`，整合层新增仅三块（生产组合根/对局外壳/UI宿主）。

---

## 六、导航（其余文档各就其位）

- **全局报告（信任与断点的唯一权威依据）**：`docs/L_归档/02_项目全局报告_历史.md`
- **并行 Prompt 集群**：`docs/并行作战/00_索引.md`（一条线一份完整 Prompt）
- **权威规范**：`docs/L0_规范宪法.md`（宪法）· `docs/访谈决策记录.md`（D-001~D-062 全部裁决）
- **裁决入口（活动）**：`docs/L_审查报告/00_并行产出裁决与整理.md`
- **审计报告（只陈述、不裁决）**：`docs/L_审查报告/*.md`
- **各层设计**：`docs/L1_引擎层/` · `docs/L2_基类层/` · `docs/L3_玩法层/`
- **历史档案（勿当路线图）**：`docs/L1_引擎层/并行审查Prompt/`（59 份，见其 `00_状态基线.md`）· `docs/L_归档/`
- **并行方法**：`docs/00_并行作战手册.md`
