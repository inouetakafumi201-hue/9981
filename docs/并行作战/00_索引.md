# 并行作战 · Prompt 索引

> **怎么用**：从下表挑一条 🟢 就绪线，把对应文件的整份 Prompt 复制到一个独立会话执行。**一条线一个会话**。
> **纪律**：🔒 线在其前置裁决落地前**不要开工**。所有产出按各 Prompt §8「回流」写回，不直接改主状态板。
> **依据**：`docs/02_项目全局报告.md`（信任与断点）、`docs/00_并行作战手册.md`（就绪门与行为契约）。
> **更新**：2026-08-13 收束批次（Wave 4 已随 U-002 裁决解锁，PT-07 转就绪，UP）

---

## 就绪线（现在就能并行，互不依赖）

| Prompt | 线 | 目标一句话 | 白名单（只改这里） |
|---|---|---|---|
| ~~[PT-02](./PT-02_l2导出UGC端口.md)~~ | L2→UGC | ✅ 已完成（2026-08-10）：`src/l2/ugc/ports/` 已交付，入口 `createL2PortBundle()`，通过 `inspectL2PortBundle`；PT-10 已解锁 | `src/l2/ugc/` |
| ~~[PT-03](./PT-03_复选框对齐现实.md)~~ | 全 | ✅ 已完成（2026-08-10）：三个 tasks.md 逐条对齐并带可核对注记，经机械校验（证据路径全部存在）。meta 125✅/26未；core-mechanics 79 全 `[ ]`（D-062 冻结+零测试）；space-items 36 全 `[ ]`（PT-03b 子代理越界扩写已回退到 PT-08a 版再最小标注） | 三个 `.kiro/specs/*/tasks.md` |
| ~~[PT-04](./PT-04_AI覆盖率审计.md)~~ | AI | ✅ 已完成（2026-08-10）：逐条核对 wakeup-ai requirement 的断言完备性，43✅/22◐/6⬜，报告落 `docs/L_审查报告/wakeup-ai-覆盖率审计.md` | `.kiro/specs/wakeup-ai/tasks.md`、审计报告 |
| ~~[PT-06](./PT-06_test纳入typecheck与lint.md)~~ | 工程 | ✅ 已完成（2026-08-09）：`tsconfig.json` include→`["src","test"]`、`lint`→`eslint src test --ext .ts,.tsx`、新增 `typecheck:l2` 与 `verify`；并落地机器守卫 `test/toolchain/gate-coverage.test.ts` 防止范围被静默缩回 | `tsconfig*.json`、`package.json` scripts、`.eslintrc.cjs`（+ 守卫落在 `test/toolchain/`、`vitest.config.ts` 的 include） |
| ~~[PT-08a](./PT-08a_按D-058重写space-items-tasks.md)~~ | L2 | ✅ 已完成（2026-08-09）：tasks.md 已重写为校验既有+补缺口，PT-08b 已解锁 | `.kiro/specs/wakeup-space-items/tasks.md` |
| [PT-08b](./PT-08b_space-items实施.md) | L2 | 按 PT-08a 新版 tasks.md 实施 space-items 基类层（73 个任务，14 个属性测试） | `src/l2/**`、`src/class/<族>/index.json`、`test/l2/space-items/**` |
| ~~[PT-09](./PT-09_UI从task0起步.md)~~ | UI | ✅ 已完成（Tasks 0-11，360 测试全绿） | `src/ui/`、渲染层 lint 配置 |
| [PT-10](./PT-10_UGC真实上游集成.md) | UGC | 完成 wakeup-ugc 任务 11.1/11.3（真实 l2 端口集成）+ 11.4–11.6（恶意输入与故障注入） | `src/core/ugc/integration/**`、`src/core/ugc/__tests__/integration/**` |
| [PT-07](./PT-07_L3核心机制收敛_阻塞.md) | L3 | L3 核心机制双实现收敛（D-062）+ 补齐必交付属性测试 | `src/play/action-turn/**`、`src/play/core-mechanics/**`、`src/play/playpack-compiler/**` |
| [PT-11](./PT-11_基类层ECS收敛.md) | L2 | 基类层 ECS 收敛（收束专项）：组件契约单一源、家族收拢为组件形状、原子 System 接线、vehicle 降级为组合型组件族（Wave 5） | `src/l2/model/{composition-registry,composition-shape,family-contracts,space-items-contracts,index}.ts`、`src/l2/validation/{composition-alignment-rules,validator}.ts`、`src/l2/index.ts`、`src/l2/决策与风险记录.md`、`test/l2/ecs/**` |

## 可选 / 低优先（不阻塞他人）

（无，PT-04 已完成）

## 阻塞线（已全部解锁）

| Prompt | 线 | 解锁条件 | 状态 |
|---|---|---|---|
| ~~[PT-01](./PT-01_执行D-061收敛_阻塞.md)~~ | L2 | D-061 批准 **且** PT-02 完成 | ✅ **已完成解锁**（D-061 于 2026-08-11 确认，PT-02 已完成） |
| ~~[PT-07](./PT-07_L3核心机制收敛_阻塞.md)~~ | L3 | D-062 批准 **且** U-002 由所有者裁决 | 🟢 **已解锁并转入就绪线**（U-002 于 2026-08-13 裁决：维持 D-037 单人 2 AP，abort 语态作废；Wave 4 随之解锁）。现列于上方「就绪线」 |

---

## 并行编组建议（一轮最多同时开这些，彼此零冲突）

**第一波（全 🟢，可同时 5 开）**：PT-02、~~PT-03~~（已完成）、~~PT-06~~（已完成）、~~PT-08a~~（已完成）、PT-09
- 冲突检查：**原先声称"白名单目录两两不相交"，这条对 PT-06 与 PT-09 并不成立**——PT-06 的白名单含 `.eslintrc.cjs` 与 `package.json` scripts，PT-09 的白名单含"渲染层 lint 配置"，两者指向同一批文件。2026-08-09 实际发生了同文件并发改动（PT-06 加范围说明注释、PT-09 把 `src/ui/**` 加进渲染层 `no-restricted-imports` override），因落点不同位置而侥幸未冲突。
- **纪律修正**：`.eslintrc.cjs` 与 `package.json` 视为**共享文件**，涉及它们的线要么串行、要么在 Prompt 里写明各自只改哪个区块。可以自由增删 override，但**不要改动 `lint` 脚本的 `src test --ext .ts,.tsx` 范围**——一改回去，`test/**` 立刻重新脱离 lint（这正是 PT-06 修的盲区）。范围如今由 `test/toolchain/gate-coverage.test.ts` 断言守着，缩回会直接让测试红。
- 其余四条线（l2/ugc、specs/tasks.md、space-items/tasks.md、src/ui 源码）白名单确实互不相交 ✅。
- PT-08a 完成后，第二波新增 🟢 **PT-08b**（按新版 tasks.md 实施；白名单 `src/l2/**`、`src/class/<族>/index.json`、`test/l2/space-items/**`）。

**第二波（第一波回流后）**：PT-01（若 D-061 已批准且 PT-02 完）、PT-07（若 D-062+U-002 已定）、PT-08b。

**2026-08-13 收束批次更新**：D-061、D-062、U-002 三项裁决均已落地。**PT-01 与 PT-07 均已完成解锁**；PT-07 已从阻塞线移至就绪线（白名单恢复为 Wave 4：`src/play/action-turn/**`、`src/play/core-mechanics/**`、`src/play/playpack-compiler/**`）。当前可并行派发的就绪线：**PT-01、PT-07、PT-08b、PT-10** 及其它尚未启动的 🟢 线。锁边界细节见 `docs/L_归档/steering_历史/PARALLEL_EXECUTION_LOCK.md`（现役 Wave 4）。

**2026-08-14 ECS 收敛批次（Wave 5）**：新增 **PT-11（基类层 ECS 收敛）**。白名单独占 `src/l2/model/**` 与 `src/l2/validation/**`，与 Wave 4（`src/play/**`）、PT-08b（`src/l2/adapters/**`、`src/l2/registry/**`、`src/class/**` 目录）、PT-10（`src/core/ugc/**`）均不相交。**共享文件提示**：PT-11 白名单含 `src/l2/model/diagnostic-codes.ts`（新增 ECS 专属诊断码）与 `src/l2/validation/{validator,classification-rules,...}`（挂新校验），PT-08b 也涉 `src/l2/**` 校验与 model 目录——两线若同改 `src/l2/validation/index.ts` / `validator.ts` / `src/l2/model/index.ts` 必须在各自 Prompt 里只追加导出、不覆盖，并在开工前 `git status` 确认无未提交变更。
