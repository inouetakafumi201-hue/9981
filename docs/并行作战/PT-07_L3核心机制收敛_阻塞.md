## 任务：PT-07 — L3 核心机制双实现收敛 + 补齐必交付属性测试

> 🔒 **解锁条件（缺一不可）**：① `docs/访谈决策记录.md` **D-062 被项目所有者批准**；② **U-002 由所有者裁决**（单人投点 D-037 单人 2AP vs abort，二选一）。未同时满足**不要开工**。

### 1. 背景与意图
`src/play/` 存在两套并行实现同一 L3 核心机制（AP/体力/过载/行动轮/招架/弱点/失衡），结论矛盾：`action-turn/playpack.json`（纯数据、30 测试、可信）与 `core-mechanics/*.ts`（TS、**零测试**、含装载期治理 Linter）。D-062 裁决：**声明式 playpack 为机制内容权威形态；core-mechanics 保留为装载期治理层作用于它**。本任务执行收敛并补齐 core-mechanics 从未编写的必交付属性测试（tasks 9–10）。

### 2. 权威依据（先读）
- `docs/访谈决策记录.md` **D-062**（收敛方向 a/b/c/d）+ **U-002 最终裁决**
- `src/play/action-turn/决策与风险记录.md`（playpack 的冲突采用决议，含 RECON-001）
- `src/play/core-mechanics/{load,ownership,allocation,projection}.ts` 与 `defs/*`（治理层与 TS defs 现状）
- `wakeup-core-mechanics/design.md` §3.17/3.18、tasks 9–10（属性测试要求：一文件一属性、`numRuns≥100`、必交付）

### 3. 就绪确认（开工前逐条核对）
- [ ] D-062 已批准
- [ ] U-002 结论已定（写明采用哪个）
- [ ] action-turn playpack 测试当前全绿（收敛基线）

### 4. 允许改动的目录（白名单）
- `src/play/core-mechanics/`（保留治理/Linter/装载入口；移除与 playpack 重复的机制 defs 或改为消费单一权威数据源）
- `src/play/action-turn/`（若定为单一权威数据源，按 U-002 结论消除矛盾）
- `src/play/core-mechanics/__tests__/property/`（**新建**：补齐 tasks 9–10 属性测试 + generators）

### 5. 禁止触碰（黑名单）
- 不改 `src/core/**`、`src/l2/**`、`src/class/**`。
- 不保留两套并行机制内容"用适配器连起来"——按 D-062 收敛为单一权威。
- 不臆造 U-002 结论——以所有者裁决为准。

### 6. 行为契约
遵循 `docs/00_并行作战手册.md` §四。先确立单一权威数据源并消除 U-002 矛盾 → 再让 core-mechanics 治理层作用于它 → 再补属性测试。属性测试不得空转（必须能命中被测子句）。

### 7. DoD（可机器校验）
- [ ] L3 核心机制只剩**单一权威数据源**；`allocation.ts` 记录的"两套结论矛盾"已消除。
- [ ] `core-mechanics` 装载/治理路径**有测试覆盖**（此前为零）。
- [ ] tasks 9–10 的必交付属性测试全部存在并通过（一文件一属性、`numRuns≥100`）。
- [ ] `npx tsc --noEmit` 0 错；`npx vitest run` 全绿；`npm run lint` 0 错。

### 8. 回流方式
- 完成后 D-062 标"已执行"，U-002 结论同步到 `known-divergences.ts` / requirements / design（消除 L3-DIV 相关分歧），主状态板 PT-07 标 ✅。
- `wakeup-core-mechanics/tasks.md` 复选框据实更新（带证据）。
