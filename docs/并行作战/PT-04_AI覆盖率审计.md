## 任务：PT-04 — wakeup-ai 覆盖率审计（可选 / 低优先，不阻塞他人）

### 1. 背景与意图
`wakeup-ai` 代码位于 `src/core/kernel/ai/`，组件与 design 逐项吻合，9 个测试文件全绿，全局报告已定为**高信任**。本任务做一次**确认性覆盖率审计**：逐条核对 requirements 的验收断言是否都有对应测试，把"高信任"从"组件齐全"升级为"逐条可证"。纯审计，不改实现。

### 2. 权威依据（先读）
- `.kiro/specs/wakeup-ai/requirements.md`（12 条需求的验收标准）
- `.kiro/specs/wakeup-ai/design.md`（不变量：认知/合法性/写入/试探/诊断）
- `src/core/kernel/ai/__tests__/*`（现有 9 测试文件）

### 3. 就绪确认
- 依赖已闭合：AI 代码与测试均已存在且全绿。
- 冻结：纯只读审计，不改代码。

### 4. 允许改动的目录（白名单）
- 仅新增审计报告 `docs/L_审查报告/wakeup-ai-覆盖率审计.md`。
- 若发现缺测且决定补测，另立子任务（新增 `src/core/kernel/ai/__tests__/` 测试），但**先出审计再补**。

### 5. 禁止触碰（黑名单）
- 不改 AI 实现代码（除非审计发现真实缺陷，那走交接项/独立修复）。

### 6. 行为契约
遵循 `docs/00_并行作战手册.md` §四与"审计只陈述、不裁决"（§五）。

### 7. DoD（可核对）
- [ ] 一张表：requirements 12 条 × [是否有测试断言覆盖 | 证据测试名 | 缺口]。
- [ ] 结论：AI 高信任是否逐条成立；列出任何缺测面作为可选补测清单。

### 8. 回流方式
- 审计事实 → `docs/L_审查报告/wakeup-ai-覆盖率审计.md`。
- 主状态板 PT-04 标 ✅（或标注发现的缺测清单）。
