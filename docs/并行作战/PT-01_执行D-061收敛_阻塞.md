## 任务：PT-01 — 执行 D-061（src/l2 与 spec-compiler 收敛）

> 🔒 **解锁条件（缺一不可）**：① `docs/访谈决策记录.md` **D-061 被项目所有者批准**（状态从"待复核"改为"已确认"）；② **PT-02 已完成**（l2 端口已交付，避免退役 spec-compiler 时打断 UGC 消费节奏）。未同时满足**不要开工**。

### 1. 背景与意图
把 L2 编译/校验/注册管线的所有权收敛到 `src/l2`，退役 `src/core/kernel/spec-compiler` 中携带基类层/玩法层语义的 `SpecificationCompiler`/`SpecificationValidator`，保留其无游戏语义的 JSON 原语于内核。见 D-061 全文。

### 2. 权威依据（先读）
- `docs/访谈决策记录.md` **D-061**（裁决全文与四项落点 a/b/c/d）
- `docs/L_审查报告/spec-compiler-缺口审计.md`（spec-compiler 独有缺口清单：`validateTechnicalQuotas`、来源优先级/未决冲突语义、废用术语四词等——这些**必须迁入 src/l2 而非丢弃**）
- 消费方证据：`src/class/catalog-loader.ts`、`src/play/profiles/*`（只用 `StrictJsonCodec`/`JsonValue`/`canonicalStringify`）

### 3. 就绪确认（开工前逐条核对）
- [ ] D-061 已批准（贴出决策记录中"已确认"字样）
- [ ] PT-02 已完成（l2 端口交付）
- [ ] 全仓测试当前全绿（改造基线）

### 4. 允许改动的目录（白名单）
- `src/core/kernel/spec-compiler/`（退役 `SpecificationCompiler`/`SpecificationValidator`；把引擎级 JSON 原语更名/保留为 `json-primitives` 或等价）
- `src/l2/`（迁入 spec-compiler 独有缺口规则）
- 引用方 import 路径：`src/class/**`、`src/play/**`（仅更新 import 到更名后的原语模块）

### 5. 禁止触碰（黑名单）
- 不改 `src/core/ugc/**`、`src/core/kernel/`（除 spec-compiler 子树与被更名原语的引用）。
- 不用适配器把两套"连起来"固化重复——是**退役+迁移**，不是适配。

### 6. 行为契约
遵循 `docs/00_并行作战手册.md` §四。分步执行，每步跑三条命令：先迁缺口入 src/l2（加测试）→ 再改引用方 import → 再退役 SpecificationCompiler → 全绿。

### 7. DoD（可机器校验）
- [ ] `SpecificationCompiler`/`SpecificationValidator` 已移除或降级为不再承载 L2 语义；其独有缺口在 `src/l2` 有等价实现**且有测试**。
- [ ] 引擎级 JSON 原语模块无"编译职责"命名暗示；`src/class`/`src/play` import 已更新。
- [ ] `npx tsc --noEmit` 0 错；`npx vitest run` 全绿（迁移的缺口规则有新测试覆盖）；`npm run lint` 0 错。

### 8. 回流方式
- 完成后把 D-061 状态在决策记录标"已执行"，主状态板 PT-01 标 ✅。
- l2 D-1/D-2/D-4/D-5 落点随之在 `src/l2` 确定，更新 `l2-base-layer-spec/tasks.md`（交接项）。
