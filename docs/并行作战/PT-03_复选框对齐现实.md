## 任务：PT-03 — 三个 spec 的 tasks.md 复选框对齐测试现实

### 1. 背景与意图
`tasks.md` 复选框与现实严重脱节，导致进度不可知：`meta-mechanism-kernel` 声称 0/151 却功能全绿；`wakeup-core-mechanics` 声称 0/79 但核心代码已存在；`wakeup-space-items` 声称 0/67 但校验已大量落地。本任务把这三个 spec 的复选框**改到与测试现实一致**，使复选框重新可信。对应主状态板 PT-03、全局报告 §二。

### 2. 权威依据（先读）
- `docs/L_归档/02_项目全局报告_历史.md`（各线真实完成度与断点——这是对齐依据）
- 各 spec 的 `tasks.md` 与其对应代码/测试目录

### 3. 就绪确认
- 依赖已闭合：全局报告已给出每条线"真实完成/未测/未做"的判断，可直接据此勾选。
- 冻结：不改任何代码，只改复选框与必要的状态注记。

### 4. 允许改动的目录（白名单）
- `.kiro/specs/meta-mechanism-kernel/tasks.md`
- `.kiro/specs/wakeup-core-mechanics/tasks.md`
- `.kiro/specs/wakeup-space-items/tasks.md`
（三个可拆成三个并行子会话，彼此零冲突。）

### 5. 禁止触碰（黑名单）
- **不改任何 `src/**` 或 `test/**` 代码**。
- 不改 requirements.md / design.md（若发现设计过时，写成交接项，不在本任务改）。
- `wakeup-space-items/tasks.md` 的**结构性重写归 PT-08a**；本任务对它只做"标注哪些已被既有代码覆盖"的复选框/注记，不重写任务体。

### 6. 行为契约
遵循 `docs/00_并行作战手册.md` §四。每勾一项必须有证据（测试文件路径或代码文件路径），在该任务行后追加一句 `（证据：<path>）`。**无证据不得勾选**。凡"声称完成但查无实据"的（如需实测确认），标 `[ ]` 并注 `待测`。

### 7. DoD（可机器校验 + 可核对）
- [ ] 三个 tasks.md 的每个 `[x]` 都带证据路径；每个仍 `[ ]` 的都注明原因（未做/待测/被裁决阻塞）。
- [ ] 特别地：`wakeup-core-mechanics` 中"必交付属性测试(9–10)"必须标 `[ ]` 并注 `D-062 冻结中：core-mechanics 零测试，等收敛`。
- [ ] 不引入任何代码改动（`git`/文件树仅 tasks.md 变化）。

### 8. 回流方式
- 完成后在主状态板把 PT-03a/b/c 标 ✅。
- 若对齐过程中发现新的"设计与现实矛盾" → 登记到裁决入口，**不自行改设计**。
