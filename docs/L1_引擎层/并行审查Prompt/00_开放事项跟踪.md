# 并行审查Prompt 目录 — 开放事项跟踪

> **建立日期**：2026-08-07
> **性质**：本目录审查过程中提出、**至今仍未闭环**的事项的唯一清单。
> **配套**：状态口径见 [`00_状态基线.md`](00_状态基线.md)；需人工裁决的事项见
> `.kiro/specs/meta-mechanism-kernel/决策与风险记录.md` 第 16 节。
> **纪律**：本目录其它文档不再保留活动待办标记。发现遗漏请补到本文件，不要写回历史报告。
>
> ### 开放项总数速览
>
> | 类别 | 数量 | 位置 |
> |---|---:|---|
> | 内核设计风险（M-1~M-13 中风险） | 13 | §1.1 |
> | 内核设计风险（L-1~L-18 低风险） | 18 | §1.2 |
> | 审查流程未执行动作（T-01~T-04、T-12） | 5 | §二 |
> | 目录外关联开放项（T-05、T-07~T-11） | 6 | §四 |
> | **合计仍开放** | **42** | — |
> | 本轮已处理的目录内文档缺陷（T-06、T-13） | 2 | §三 |
>
> **不要把 §1.2 的 L-5/L-6/L-7 与 `REVIEW_BATCH2` 的 `❓` 重复计数**（同源，已去重）。

---

## 一、内核设计风险项（来源：13 层并行审查，2026-08-05）

出处统一为 `ENGINE_KERNEL_13LAYER_REVIEW_SUMMARY.md`（原始发现见 `REVIEW_BATCH1/2/3`）。
层号为**方案 A（Spec 章节审查轴）**，映射见 `00_状态基线.md` §2.1。

高风险 H-1/H-2/H-3 的**规范文本部分已闭环**（Spec §1.2.1、§5.1、§8.4.1、§12.0.1、§12.X 实际存在）；
**错误码部分已裁定不实现**，见 `00_状态基线.md` §四。H-2 的防御充分性仍待裁决（决策与风险记录 U-03）。

### 1.1 中风险（13 项，全部开放）

| ID | 层(A) | 事项 | 优先级 | 现状核实（2026-08-07） |
|---|---|---|---|---|
| M-1 | L1 | Tag 带参数的层级语法未规范 | P1 | 未核实 |
| M-2 | L1 | Def 继承语义冲突无检测机制（需 Linter 支持） | P1 | 相关：Linter 覆盖缺口见 §四 T-05 |
| M-3 | L2 | `spread` 的 budget 缺省值未给定 | P2 | **确认仍开放** — Spec:539 只有 `spread(origin, budget, opts?)` 形参，全文无缺省值定义 |
| M-4 | L3 | `prefab.spawn` 大型拓扑性能影响 | P2 | 未核实（需压测数据，非读码可判） |
| M-5 | L4 | modify 阶段无 payload 修改次数限制 | P2 | 未核实 |
| M-6 | L4 | 32 层 depth 上限可能不足，建议 PlaypackDef 可配 | P2 | **确认仍开放** — Spec 全文无 `maxDepth` 声明 |
| M-7 | L5 | step 预算分配策略未细化 | P2 | 未核实。同源命中：`REVIEW_BATCH2_L5-L8.md:53`（不同 phase 的 step 消耗未明确区分） |
| M-8 | L6 | queryActions 性能问题（大状态全表扫描） | P1 | 未核实（需压测数据） |
| M-9 | L6 | range 展开爆炸 | P2 | 未核实 |
| M-10 | L8 | aura 重算频率 | P2 | 未核实 |
| M-11 | L10 | ShadowRNG 种子派生规则未定义 | P2 | **确认仍开放** — Spec 全文无 `ShadowRNG`／`shadow`／影子流种子派生的任何提及 |
| M-12 | L12 | snapshot 压缩策略未定义（增量快照） | P2 | **确认仍开放** — Spec 全文无 snapshot 压缩或增量快照规范 |
| M-13 | L13 | log 截断后信息丢失风险，需关键日志保护 | P2 | **部分实现，Spec 未对齐** — `kernel-l13-test/L13_TEST_REPORT.md` 属性 #4「容量满后优先丢弃 info、error 不丢弃」PASS；Spec §3.2.1/§9.1 仍只有 `logRetention` 无差别截断 |

### 1.2 低风险（18 项，全部开放）

清单原文见 `ENGINE_KERNEL_13LAYER_REVIEW_SUMMARY.md` §3.3（现 `:167-184`），不在此重复抄录。

`REVIEW_BATCH2_L5-L8.md` 用 `❓` 标了 4 处"未明确"，它们**不是独立的第 32~35 项**，
而是已被下表的 M-7 / L-5 / L-6 / L-7 覆盖 —— 统计开放项时不要重复计入：

| ID | 层(A) | 事项 | 同源命中 |
|---|---|---|---|
| L-5 | L5 | Hook 执行是否计入 step 预算未明确 | `REVIEW_BATCH2_L5-L8.md:77` |
| L-6 | L5 | `prefab.spawn` 是否计入 step 预算未明确 | `REVIEW_BATCH2_L5-L8.md:54` |
| L-7 | L6 | queryActions 缓存机制缺失 | `REVIEW_BATCH2_L5-L8.md:183` |
| L-1~L-4、L-8~L-18 | 各层 | 见汇总报告 §3.3 | — |

> **本文件所有行号均为 2026-08-07 的状态。** 若行号对不上，以**章节标题**定位为准
> （本次修订给多个文件插入了顶部横幅，历史文档中记载的旧行号已普遍位移）。

---

## 二、审查流程遗留的未执行动作

| ID | 事项 | 出处 | 现状 |
|---|---|---|---|
| T-01 | **Spec 全文引用闭合检查**（符号表 vs 正文引用差集） | `P0_MERGE_COMPLETE_REPORT.md` §9.1 第 2 项（`:266`） | 从未执行。该报告 §8.2（`:248`）记「引用悬空 11 处」，但 Spec 此后经过多轮修改（含"Spec 向实现对齐"整合），**11 这个基数已不可信，不要再引用**。需重跑才能得出当前数字 |
| T-02 | **`docs/L0_规范宪法.md` 增加 Ref 前缀审批流程** | `P0_FIXES_INTEGRATION_REPORT.md` §6.1 第 3 项（`:185`） | **确认未做** — `docs/L0_规范宪法.md`（244 行）全文无「前缀」条款；审批流程目前只存在于 Spec §1.2.1。宪法本身声明变更需人工审批，故本轮不代改 |
| T-03 | **跨层一致性四项检查** | `EXECUTE_ALL_TESTS.md`「跨层一致性检查」（`:211-214`），另见「成功标准」第 5 项（`:232`） | 从未执行。这是唯一的跨层门禁：① L11 错误码注册表是否覆盖 L1–L10 实际抛出的全部码；② L3 cost 三态 与 L10 Intent 冻结/结算/退回 语义是否一致；③ L7 级联删除 与 L8 Relation 清理 是否互相留下悬空引用；④ L4 depth 上限 与 L9 reactionRounds 是否互相绕过。（层号为方案 C）<br>**根因**：12 个 `kernel-l*-test` 是彼此独立的 npm 子项目、互不 import，也没有统一 CI 入口，天然无法验证跨层一致性 |
| T-04 | **L4 Hook 四项精化点** | `CONSOLIDATION_L3-L6_FINAL.md` §四（`:161-164`，四条未勾选）；原始 12 项记录在 `TEST_L4_Hook五阶段竞争_审查结果.md` §4.2~4.5 | 开放。① instead 竞争排序键完全相同时的 tie-breaker；② reactionRounds 与 depth 的精确定义（与 M-6 相关）；③ 无 hookId 的 Hook 的重入锁处理；④ 调用栈与事务回滚的交互 |
| T-12 | **Intent 的三个维度缺专项测试**：`hidden` 隐藏机制、`resolveOrder` 排序、`onConflict` 多策略 | `TEST_L7_Intent提交解算分离_框架.md` §一 曾规划这三项；`kernel-l10-test` 的 14 项测试聚焦 INV-12 冻结守恒，未覆盖它们 | 开放。`intent.reveal` 已实现并接入 veto 分发，但排序与冲突策略未被随机化验证。Spec §7.6 有定义，属"有规范无测试" |

---

## 三、本目录自身的文档缺陷（本轮已全部处理）

| ID | 事项 | 现状 |
|---|---|---|
| T-06 | `TEST_L3_Ops事务守恒性.md` 的 `L3-STACK-006` / `L3-INV-007` 两条用例断言与 Spec §4.7 相反 | ✅ **已修复**（2026-08-07）。原断言假设 `destroy` 不可回滚，Spec §4.7 定稿为**完全可回滚**。改动：`L3-STACK-006` 的 Then 改为「`i_1` 存在且 `stack: 10`（`:192`）／`coin总量 == 10`（`:195`）」；`L3-INV-007` 补上「`entity_1` 存在（`:1131`）／`entity_1.place == { node: node_1 }`（`:1132`）」两条回滚后状态断言。原断言以引用块保留供溯源 |
| T-13 | `TEST_L3_审查结果报告.md` 中「`L3-STACK-007` 判定 FAIL」的撰写残留块，与紧随其后的批量小节「`L3-STACK-007`: PASS」及统计表 FAIL=2 三方矛盾 | ✅ **已处理**（2026-08-07）：残留块加 ⛔ 标记作废，明确以 PASS 为准并说明两者情形不"同理"。**未删除原文**，保留供溯源 |

---

## 四、目录外的关联开放项（登记备查，不在本目录修复）

这些事项在本次审计中被实测发现，但落在本目录范围之外。列出以免丢失，修复责任在各自所有者。

| ID | 事项 | 证据 | 所有者 |
|---|---|---|---|
| T-05 | **Linter 未覆盖 requirements 39.11 的九类装载期检查**：缺类型一致性（深度版）、具名表达式调用图无环（`E_EXPR_CALL_CYCLE`）、`aura.deps` 完整性三类；`L13_TEST_REPORT.md` 明确指出**主实现 `src/core/kernel/safety/safety.ts` 存在相同缺口**（注释按 1/2/3/5/8/9 编号，遗漏 4/6/7）。与 M-2 相关 | `kernel-l13-test/L13_TEST_REPORT.md` §已知缺口 第1条 | 内核实现 |
| T-07 | **`KERNEL_TEST_SUMMARY.md` 未纳入 L12/L13**：标题与合计仅覆盖 L1–L11（310 项），而 `kernel-l12-test/L12_TEST_REPORT.md`（18 项）与 `kernel-l13-test/L13_TEST_REPORT.md`（15 项）均已完成且 PASS | 三文件时间戳：汇总 07:22、L12 07:10、L13 07:23 | 测试汇总所有者 |
| T-08 | **`L13_TEST_REPORT.md` 自身总数不一致**：表头写 76,000 次，结论段写 55,000+ 次 | 该文件 `:11` 与 `:50` | L13 报告所有者 |
| T-09 | **配额检查存在两种边界语义且文档未说明**：`Linter.quotas` 用 `count > limit`（允许等于上限），`QuotaEnforcer.checkEntityQuota` 用 `count >= limit`（达到即拒绝）。报告判定为合理差异（装载期静态校验 vs 运行期动态拦截），但未在 design.md/requirements.md 显式澄清 | `kernel-l13-test/L13_TEST_REPORT.md` §已知缺口 第3条 | Spec/需求所有者 |
| T-10 | **两份规范宪法结构性分叉**：`docs/L0_规范宪法.md`（244 行，08-06 12:45，按「第一条~第十二条」组织，且缺「第十条」）与 `.kiro/steering/规范宪法.md`（173 行，08-05 17:08，按「一、二、三…」组织）内容组织完全不同，无交叉引用说明何者权威 | 两文件章节标题实测 | 宪法所有者（变更需人工审批） |
| T-11 | **Spec 章节编号缺陷五处**（`12.X` 占位、§4.3 标题缺失、§8.4.1 孤立、§12.0.1/§12.1 错位、被声称存在的 §9.1.1 实际不存在） | 详见 `决策与风险记录.md` 第 16 节 U-07 | Spec 所有者 |

---

## 五、已确认闭环、不再跟踪的事项

登记于此，防止后续审计重复提出。

| 原编号 | 事项 | 闭环依据 |
|---|---|---|
| P0-1 ~ P0-12 | `综合执行报告_完整版.md` 的 12 项 P0（事务回滚、INV-1 时点、preventExcept、containerIndex、depth vs reactionRounds、null 传播、类型系统、未绑定变量、查询纯度、DecisionDef 重写、Decision 状态机、quorum 语义） | 四层第二轮复测 202/202；Spec §4.3.2 / §4.7 / §6.1-6.5 / §3.1.2-3.1.6 / §7.5 实测存在 |
| H-1 / H-2 / H-3（规范文本部分） | Ref 前缀审批、Journal 权限边界、跨 Layer 状态同步 | Spec §1.2.1（:129）、§12.X（:2136）、§5.1（:1104）、§8.4.1（:1800）、§12.0.1（:2085）实测存在 |
| L5 的 17 条 UNDEF（含 5 条 P0） | null 传播、类型混合、未绑定变量、浮点精度、查询副作用、变量类型、幂运算、溢出等 | Spec §3.1.2 ~ §3.1.6 实测存在；`TEST_L5_审查结果报告.md` 第二轮 47/47 |
| L6 的 14 条 UNDEF + DEC-G1~G6 | Decision 单/多选、超时双制、撤销、askee 失效等 | Spec §7.5 七处合并；`TEST_L6_审查结果报告.md` 第二轮 30/30 |
| L5 测试断言修订（除零返回 null） | `RESULT_L5_Expr表达式求值审查报告.md` 的待办第 1 项 | `TEST_L5_Expr表达式求值.md` 规则 `EXPR-2`（`:33`）与用例 `L5-EXPR-003`（`:93`）／`L5-EXPR-004`（`:113`）已改为「返回 `null`」 |
| 阶段三 L7–L13 验收 | — | 13 层属性实测全部 PASS，见 `00_状态基线.md` §3.2 |
| P0_MERGE 的「为 8 个错误码编写测试用例」 | `P0_MERGE_COMPLETE_REPORT.md:232` | **失效而非完成**：那 8 个码已裁定不实现（`决策与风险记录.md` 第 15 节），无可测对象 |
