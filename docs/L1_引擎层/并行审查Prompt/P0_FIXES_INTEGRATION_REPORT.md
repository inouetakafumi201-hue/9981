# P0高风险项修复集成报告（方案阶段，已被合并报告取代）

> ## ⚠️ 本文件的「Spec 修改点」位置声明有误，且错误码部分已作废
>
> **实际合并结果见 [`P0_MERGE_COMPLETE_REPORT.md`](P0_MERGE_COMPLETE_REPORT.md)。**
> 本文件是**方案设计阶段**的集成报告，记录的是"打算改哪里"，不是"实际改在哪里"。
>
> **已核实的三处偏差**（2026-08-07 对 Spec 全文检索）：
>
> | 本文件声明 | Spec 实际 |
> |---|---|
> | 新增 `§9.1.1 定时器与状态失效` | **不存在**。Spec 无 §9.1.1，也无该标题文本。该功能实际落在 `§8.4.1 光环失效与计时器清理`（Spec:1800） |
> | 新增 `§12.1 Snapshot包含pending事件` | **编号不符**。Spec §12.1 是「版本迁移」（Spec:2103）；Snapshot 含 pending 在 **§12.0.1**（Spec:2085） |
> | H-2 新增错误码 `E_LOAD_FORBIDDEN_ACCESS`/`E_LOAD_FORBIDDEN_OP`/`E_DEBUG_DISABLED` | **三码均不存在**，且与 `P0_MERGE_COMPLETE_REPORT.md` 声明的 `E_JOURNAL_*` 三码互相矛盾 —— 两套命名都没有实现 |
>
> **错误码整体作废**：本文件 §3.3 列出的 5 个新增错误码，实现阶段裁定不实现
> （`决策与风险记录.md` 第 15 节）。规范文本（§1.2.1、§5.1、§8.4.1、§12.0.1、§12.X）确实已合并。
> 详见 [`00_状态基线.md`](00_状态基线.md) §四。
>
> **§6.1 的 ⏳ 事项**：3 项已完成、1 项确认未做（更新 L0 规范宪法），已就地标注。

> **修复完成日期**：2026-08-05  
> **修复范围**：3个P0高风险项  
> **修复方式**：并行方案设计 + 集成报告

---

## 一、修复项总览

| ID | 风险项 | 方案文件 | 状态 |
|----|--------|---------|------|
| H-1 | Ref前缀新增无审批机制 | [FIX_H1_Ref前缀审批机制.md](FIX_H1_Ref前缀审批机制.md) | ✅ 完成 |
| H-2 | Journal读权限边界未明确 | [FIX_H2_Journal权限边界.md](FIX_H2_Journal权限边界.md) | ✅ 完成 |
| H-3 | 跨Layer状态同步机制缺失 | [FIX_H3_跨Layer状态同步.md](FIX_H3_跨Layer状态同步.md) | ✅ 完成 |

---

## 二、各项修复概要

### H-1: Ref前缀新增审批机制

**核心修复**：
- 建立四阶段审批流程（提案→宪法审查→影响评估→批准归档）
- 明确三条完备性判据的验证方法
- 提供拒绝案例澄清边界
- 记录10个已批准前缀的决策依据

**Spec修改点**：
- §1.2后新增§1.2.1（前缀新增审批机制）
- §4.6不变量增加2条
- §13.4新增2个错误码

### H-2: Journal读权限边界

**核心修复**：
- 设计三角色权限模型（表现层/玩法包/调试器）
- 建立world.log与journal的双轨机制
- 明确AI搜索的隔离保证（试探Event不进log）
- 防御4种典型作弊场景

**Spec修改点**：
- §12后新增§12.X（Journal权限模型）
- §3.2.1补充Journal与log的关系说明
- §4.6不变量增加3条
- §13.4新增3个错误码

### H-3: 跨Layer状态同步机制

**核心修复**：
- 定义PendingEffect结构（补全§5中引用但未定义的结构）
- 建立TOCTOU重检机制（source存在性、validityCheck、Ref有效性）
- 明确snapshot包含pending状态
- 定义每phase清理失效pending effect的规则

**Spec修改点**：
- §5后新增§5.1（延时Effect与状态一致性）
- §9.1后新增§9.1.1（定时器与状态失效）
- §12后新增§12.1（Snapshot包含pending事件）
- §4.6不变量增加1条

---

## 三、Spec修改汇总

### 3.1 新增章节

| 计划章节 | 标题 | 行数 | **Spec 实际落点（2026-08-07 核实）** |
|------|------|------|------|
| §1.2.1 | 前缀新增的审批机制 | ~60行 | ✅ §1.2.1（Spec:129），编号一致 |
| §5.1 | 延时Effect与状态一致性 | ~40行 | ✅ §5.1（Spec:1104），编号一致 |
| §9.1.1 | 定时器与状态失效 | ~30行 | ❌ **§9.1.1 不存在**；功能落在 §8.4.1 光环失效与计时器清理（Spec:1800） |
| §12.1 | Snapshot包含pending事件 | ~20行 | ❌ **编号被占用**；实际是 §12.0.1（Spec:2085），§12.1 是「版本迁移」 |
| §12.X | Journal权限模型与安全边界 | ~120行 | ⚠️ §12.X（Spec:2136）—— `12.X` 是**未定稿的占位章节号**，见跟踪项 T-11 |

**合计**：~270行新增内容

### 3.2 §4.6不变量扩充

| 修复项 | 新增不变量数 |
|--------|-------------|
| H-1 | 2条（前缀集合封闭、Id命名空间隔离） |
| H-2 | 3条（Journal私有性、log窗口有界、试探Event隔离） |
| H-3 | 1条（pending事件一致性） |
| **合计** | **6条** |

### 3.3 §13.4错误码扩充

> ⛔ **本小节整表作废**：下列 5 个错误码**全部从未进入实现**，
> 实现阶段裁定不补（`决策与风险记录.md` 第 15 节）。
> 且 H-2 这一行与 [`P0_MERGE_COMPLETE_REPORT.md`](P0_MERGE_COMPLETE_REPORT.md) §2.3
> 声明的 `E_JOURNAL_FORBIDDEN_ACCESS`/`E_JOURNAL_DEBUGMODE_REQUIRED`/`E_JOURNAL_PLAYPACK_VIOLATION`
> **互相矛盾** —— 两套命名都没实现，Spec §13.4 的「已废弃码」表记录的是后者。

| 修复项 | 计划新增错误码 | 现状（`src/core/kernel/state/error-codes.ts`） |
|--------|-----------|---|
| H-1 | `E_REF_UNKNOWN_PREFIX`、`E_REF_FORBIDDEN_EXTEND` | ❌ 均不存在。`E_REF` 实为 `MISSING/KIND/DESTROYED/ABSTRACT/AMBIGUOUS/PROVIDER_CONTRACT/CYCLE` |
| H-2 | `E_LOAD_FORBIDDEN_ACCESS`、`E_LOAD_FORBIDDEN_OP`、`E_DEBUG_DISABLED` | ❌ 均不存在，且与合并报告的命名冲突 |
| H-3 | （无，使用现有错误码） | ✅ 无冲突 |
| **合计** | **5个（均未实现）** | — |

> **这不影响 H-1/H-2/H-3 的防御有效性**：审批机制、权限隔离、TOCTOU 重检都是
> **接口层面的"不可表达"**约束，不依赖运行期错误码。
> 但 H-2 的防御是否仅靠"接口不暴露"就足够，仍待裁决 —— `决策与风险记录.md` 第 16 节 U-03。

---

## 四、安全验证矩阵

### 4.1 H-1防御验证

| 威胁 | 防御机制 | 状态 |
|------|---------|------|
| 随意新增前缀 | 四阶段审批流程 | ✅ |
| 含玩法语义的前缀 | 宪法审查（不含语义判据） | ✅ |
| 前缀体系膨胀 | 影响评估阈值（≤5章节、≤10 Op） | ✅ |
| 破坏封闭性 | Linter静态检查 | ✅ |

### 4.2 H-2防御验证

| 威胁 | 防御机制 | 状态 |
|------|---------|------|
| 时间倒流作弊 | Journal对玩法包不可见 | ✅ |
| 无限回溯 | checkpoint/restore不在玩法包Op集合 | ✅ |
| 窃取AI思考 | 试探Event不进world.log | ✅ |
| 篡改Journal回放 | 玩法包无replay权限 | ✅ |

### 4.3 H-3防御验证

| 威胁 | 防御机制 | 状态 |
|------|---------|------|
| 定时炸弹被拆除 | source失效检测 | ✅ |
| 延时治疗目标死亡 | Ref悬空安全返回 | ✅ |
| 条件触发器失效 | validityCheck机制 | ✅ |

---

## 五、兼容性影响评估

### 5.1 对现有用例的影响

| 修复项 | 影响用例数 | 说明 |
|--------|-----------|------|
| H-1 | 0 | 现有用例仍用10个已批准前缀 |
| H-2 | 0 | world.log访问方式不变，Journal本就不暴露 |
| H-3 | 0 | 补全了结构定义，不改变语义 |
| **合计** | **0** | **完全后向兼容** |

### 5.2 对工程实现的影响

| 修复项 | 工程负担 | 优先级 |
|--------|---------|--------|
| H-1 | 低（主要是流程规范） | P1 |
| H-2 | 中（需实现权限隔离） | P0 |
| H-3 | 中（需实现PendingEffect存储与清理） | P0 |

---

## 六、下一步行动

### 6.1 立即行动（本次commit）

1. ✅ 生成三个修复方案文档
2. ✅ 将修复内容合并到`元机制内核Spec_v1.md`
   —— 已完成，见 [`P0_MERGE_COMPLETE_REPORT.md`](P0_MERGE_COMPLETE_REPORT.md)（规范文本部分；错误码部分未实现）
3. ❌ **更新`L0_规范宪法.md`（增加前缀审批流程）—— 至今未做**
   —— 2026-08-07 核实：`docs/L0_规范宪法.md`（244 行）全文无「前缀」条款；
   审批流程目前只存在于 Spec §1.2.1。宪法自身规定变更需人工审批，故未代改。
   已登记为 [`00_开放事项跟踪.md`](00_开放事项跟踪.md) 的 **T-02**
4. ✅ 更新审查汇总报告（标记P0已修复）
   —— 已完成，见 `ENGINE_KERNEL_13LAYER_REVIEW_SUMMARY.md` §3.1

### 6.2 后续行动（工程规范阶段）

1. **实现Journal权限隔离机制（H-2）** —— ⚠️ 部分：Journal 未对玩法包暴露（接口层隔离成立），
   但计划中的运行期拒绝码 `E_JOURNAL_*` 未实现。防御充分性待裁决（U-03）
2. ✅ **实现PendingEffect存储与清理（H-3）** —— Spec §5.1 定义 + §8.4.1 级联清理 +
   §12.0.1 快照包含 pending；`kernel-l12-test` 的 checkpoint/restore 往返一致性属性测试 PASS
3. ❌ **建立Ref前缀审批的实际流程（H-1）** —— 流程文本在 Spec §1.2.1，
   但宪法侧未落条款（跟踪项 T-02），且 Linter 未实现前缀封闭性拦截
4. ⚠️ **编写对应的单元测试** —— 各层属性测试已覆盖 13 层，但**这三项的专项测试未单列**；
   `P0_MERGE_COMPLETE_REPORT.md:232` 计划的「为新增 8 个错误码编写测试用例」
   因错误码不实现而**失效**（无可测对象），不是遗漏

---

## 七、审批记录

| 审批项 | 决策 | 理由 |
|--------|------|------|
| H-1修复方案 | 通过 | 四阶段流程明确，历史记录完整 |
| H-2修复方案 | 通过 | 三角色权限模型清晰，防御验证完整 |
| H-3修复方案 | 通过 | 补全结构定义，TOCTOU重检机制完整 |

---

**报告生成时间**：2026-08-05  
**报告生成者**：Claude Opus 4.8 (Anthropic)  
**文档版本**：P0高风险项修复集成报告 v1.0  
**文档状态**：🗄️ 历史归档（方案阶段）—— 已被
[`P0_MERGE_COMPLETE_REPORT.md`](P0_MERGE_COMPLETE_REPORT.md) 取代

**三项P0修复方案全部完成**，已进入并完成 Spec 合并阶段（规范文本部分）。
**未完成的尾巴**：宪法侧前缀审批条款（T-02）、Spec 引用闭合检查（T-01）、
H-2 防御充分性裁决（U-03）。见 [`00_开放事项跟踪.md`](00_开放事项跟踪.md)。
