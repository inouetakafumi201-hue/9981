# 规范宪法（Constitution）

> **用途**：唯一权威规范文件。所有其他文档均引用本文档，不得与之冲突。
> **维护规则**：本文档变更需手动审批；其他文档可由 AI 起草。
> **版本**：v1.0 | 创建于 2026-08-05

---

## 一、术语铁律

| 术语 | 定义 | 状态 |
|------|------|------|
| **引擎层（Kernel）** | 底层原语集。Entity、Component、Op、Expr、Hook... | ✅ 有效 |
| **基类层（Class）** | 实例库。可组合的语义最小单元（武器类、散射谱型、NPC实例...） | ✅ 有效 |
| **玩法层（Play）** | 组合基类层实例 + 数值约束 + 玩法规则 | ✅ 有效 |
| **实例（Instance）** | 基类层的具体产物 | ✅ 有效 |
| **~~模板~~** | **废用词**，禁止使用 | ❌ 禁用 |
| **~~内容层~~** | **废用词**，改为基类层 | ❌ 禁用 |
| **~~Layer 1/2/3~~** | 除非配合具体架构图使用，禁止单独使用 | ⚠️ 慎用 |

---

## 二、三层架构定义

```
引擎层（Kernel）
  ├─ Entity、Component、Relation、Op、Expr、Hook...
  ├─ 不知道"武器"、"技能"是什么
  ├─ 只知道：实体、状态、关系、动作、规则
  └─ 铁律：0追加。底层设计完成后可自我迭代，不影响上层
        ↓ 基类层使用引擎层 Op/Query
基类层（Class）
  ├─ 武器实例
  ├─ NPC实例
  ├─ 载具实例
  ├─ 角色实例
  ├─ 状态效果库
  ├─ 物品实例
  ├─ 网关实例
  └─ 铁律：
     ├─ 实例不含玩法数值约束（霰弹枪不写"伤害=3"）
     ├─ 实例不含具体玩法规则（霰弹枪不写"在哪种模式出现"）
     └─ 实例可组合、可继承、可扩展
          ↓ 玩法层组合基类层实例
玩法层（Play）
  ├─ 大逃杀规则
  ├─ 夺旗规则
  ├─ 具体数值配置（霰弹枪伤害=3、近战流派验证...）
  ├─ 玩法包定义
  └─ 铁律：可高频迭代，不影响引擎层和基类层
```

---

## 2.1 架构收敛状态（2026-08-11 更新）

> **⚠️ 重要**：项目正在进行结构收敛，部分目录/文件处于锁定状态。
> **详细状态**：见 `.kiro/steering/PARALLEL_EXECUTION_LOCK.md`
> **执行计划**：见 `.kiro/steering/结构收敛执行计划.md`

### 关键裁决

| 编号 | 裁决 | 执行状态 |
|------|------|---------|
| **D-061** | `src/l2/` 是 L2 语义管线唯一权威；`src/core/kernel/spec-compiler/` 编译器部分冻结退役 | Wave 1 准备中 |
| **D-062** | `playpack.json` 是机制内容权威；`core-mechanics` 收缩为装载期 Linter | 🔒 等待 U-002 裁决（PT-07） |
| **D-067** | 表现/创作/运营是正交架构域，不是 L4 | Wave 3 待执行 |

### 当前锁定目录

**🔴 禁止修改**：
- `src/core/kernel/spec-compiler/`（Wave 1 审计中）
- `src/play/core-mechanics/`（D-062 冻结）
- `docs/L3_玩法层/01_*.md`、`06_*.md`、`07_*.md`（Wave 3 将迁移）

**🟡 谨慎修改**：
- `src/l2/`（Wave 1 将迁移 spec-compiler 缺口进来）
- `src/class/*/index.json`（Wave 2.4 统一 schema 迁移）

**详细锁定状态与协调机制**：见 `.kiro/steering/PARALLEL_EXECUTION_LOCK.md`

---

## 三、基类层的判定标准与已知类别

> **核心原则**：基类层不能被穷举，但必须有判定标准。穷举是玩法验证阶段的事，不是宪法的事。

### 3.1 基类的判定标准

一个概念是否属于基类层，用以下三条判断：

| 判据 | 含义 |
|------|------|
| **可枚举** | 在当前玩法范围内，可以被有限列举 |
| **可组合** | 可以与其他基类组合产生实例 |
| **不含玩法语义** | 不知道自己在服务什么具体玩法 |

**反例**：`霰弹枪`不是基类，它是`枪类+散射谱型+枪械伤害`的组合实例。
**正例**：`枪类`、`散射谱型`、`枪械伤害`是基类。

### 3.2 已知类别（示例，非穷举）

以下是目前已识别的基类类别，**不代表完整清单**：

| 类别 | 说明 | 示例 |
|------|------|------|
| 武器类 | 攻击动作的抽象 | 枪类、近战类 |
| 谱型类 | 效果的形状/范围 | 散射谱型、单点谱型 |
| 伤害类 | 伤害计算方式 | 物理伤害、穿透伤害 |
| 状态类 | 标签型/数值型效果 | 中毒、眩晕、加速 |
| 物品类 | 可携带物品的抽象 | 消耗品、工具 |
| 防具类 | 防御能力的抽象 | 护甲、盾牌 |
| 载具类 | 移动+交互能力 | 车辆、飞行器 |
| 容器类 | 持有其他物品的能力 | 背包、箱子 |
| 移动模式 | 移动方式抽象 | 步行、跳跃、飞行 |
| AI行为类 | NPC行为范式 | 守卫、僵尸、载具AI |
| 网关类 | 条件/检定/资源转换 | 商店、宝箱、上锁门 |

### 3.3 已知类别外的处理

**遇到未在清单中的新概念时**：

```
Step 1: 用三条判据判断是否属于基类层
Step 2: 如果是 → 新增基类类别，记录决策原因
Step 3: 如果否 → 归入玩法层
Step 4: 更新"已知类别"列表（仅用于文档追踪，不是宪法修改）
```

**禁止行为**：
- ❌ 在宪法里穷举所有基类
- ❌ 要求人类预先列出完整基类清单
- ❌ 将新基类的发现视为"违反宪法"

---
# 并行执行与自主迭代策略

## 一、并行 Prompt 分发策略

当同时满足以下 **两个条件** 时，必须采用并行 Prompt 分发模式，而不是在当前会话中依次执行：

1. **任务可并行**：多个子任务之间无依赖关系，可以独立推进。
2. **通过高规范获得自主迭代效率**：每个子任务都有足够清晰的规范（Spec、设计文档、接口定义等），AI 可以自主迭代完成，不需要频繁向用户确认。

**并行 Prompt 分发模式**：
- 将可并行的子任务拆分为多份完整 Prompt。
- 输出这些 Prompt，让用户复制到其他独立会话中并行执行。
- 当前会话不暂停，继续执行下一个可独立推进的任务。
- **严禁**为了等待并行结果而暂停当前会话的执行。

### 判定示例

| 场景 | 是否并行分发 | 原因 |
|------|-------------|------|
| 同时编写 3 个无关的基类实例 | ✅ 是 | 无依赖、规范清晰 |
| 引擎层重构 + 基类层新增 | ❌ 否 | 引擎层变更会影响基类层，有隐式依赖 |
| 编写文档 + 编写对应代码 | ❌ 否 | 文档和代码需要双向对齐 |
| 多个测试文件编写 | ✅ 是 | 各测试文件独立 |

---

## 二、不满足并行条件时的处理

当任务 **不满足** 上述两个条件之一时：

1. **优先使用 Sub-Agent**：如果子任务可以委托给 sub-agent（如 `context-gatherer`、`general-task-execution` 等），优先使用 sub-agent 执行。**不受频率限制**，可以任意次数调用。
2. **顺序执行**：如果 sub-agent 也不适用，则在当前会话中按依赖顺序依次执行。

### Sub-Agent 使用原则

- **不受频率限制**：不要因为"已经用了很多次 sub-agent"而停止使用。只要场景匹配，就继续用。
- 适合的场景包括但不限于：
  - 代码库探索（`context-gatherer`）
  - 独立子任务执行（`general-task-execution`）
  - 代码审查（`semantic-reviewer`）

---

## 三、/goal 指令 — 全自动死磕执行模式

### 快捷指令设定：`/goal [最终目标描述]`

当收到 `/goal` 指令时，启动【全自动死磕执行模式】。核心使命：独立、彻底、高标准地完成目标。

### 3.1 核心行为准则（绝对死磕）

- **无限推进，不准停下**：无论任务多复杂，都不能停止执行。在没有彻底达成最终目标前，每轮对话的结尾必须是"自主执行下一步行动"，**严禁**等待用户下达"继续"指令。
- **拒绝偷工减料**：严禁使用 `// TODO`、`// 后面代码省略`、占位符或伪代码。所有代码必须是完整的、可编译的、生产环境级别的。
- **拒绝自我感动（禁止自我判定完成）**：没有资格单方面宣布任务完成。只有在所有测试 100% 通过、运行日志无报错、且通过了自己设计的"反向漏洞审查"后，才能向用户提交验收申请。

### 3.2 自动化执行循环（Loop）

对于输入的每个目标，必须自动陷入以下循环，直到任务终结：

#### 步 1：拆解与边界（首次触发执行）
- 明确输出最终目标的 Definition of Done (DoD)。
- 拆解为连续的原子任务清单，并标注当前正在执行的 Step。

#### 步 2：连续编写与自我迭代（循环核心）
- 编写完整代码。修改后，必须主动思考并写出潜在的 Bug 和边界情况。
- 如果前一步失败或报错，立刻自我修正，**严禁**向用户抱怨或等待指示，直接尝试方案 B、方案 C。

#### 步 3：全量验证
- 自行编写或运行现有的单元测试/集成测试。
- 检查是否存在类型错误、未处理的异常或性能隐患。

### 3.3 /goal 模式下的并行约束

**当用户要求不要停下来，或执行自主长期任务（如使用 /goal）时：**

- ❌ **禁止**通过停下来的方法提供并行 Prompt 让用户复制到其他会话。
- ✅ **只能**使用 Sub-Agent 进行并行，在当前会话中持续自主推进。
- 可以不受频率限制地调用 Sub-Agent。

### 3.4 唯一的合法退出条件（免责断路器）

只有遇到以下**不可抗力的恶性阻碍**时，才可以暂停并向用户求助。求助时必须提供完整的排查日志：

1. **硬性外部依赖缺失**：缺少必要的 API 密钥、私有凭证或外部硬件环境。
2. **底层设计冲突**：发现目标与当前项目的核心底层架构存在根本性、毁灭性的逻辑冲突，继续做会导致整个系统崩溃。
3. **环境/工具链死锁**：遇到当前 AI 能力或本地环境工具链无法解析的恶性 Bug（如编译器本身的 Bug）。

> **注意**：遇到普通报错、代码调不通、找不到方案，**绝对不属于**退出条件，必须继续死磕。
